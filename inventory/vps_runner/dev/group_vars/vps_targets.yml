---
# dev environment overrides.
# Add keys here to override values in common/group_vars/vps_targets.yml.
# Ansible merges both inventory sources (-i common -i dev); dev takes precedence.
#
# Example dev-only overrides:
#   features:
#     swap: true
#     network_tuning: true
