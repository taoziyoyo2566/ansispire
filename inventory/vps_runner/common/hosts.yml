---
# No hosts here — common/ is a shared group_vars layer only.
# Ansible requires at least one parseable inventory file in a directory
# for -i <dir> to load its group_vars. The actual host roster lives in
# inventory/vps_runner/<env>/hosts.yml.
all:
  children:
    vps_targets: {}
