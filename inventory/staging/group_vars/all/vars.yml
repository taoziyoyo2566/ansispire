# staging overrides on top of production defaults

---
env: staging

# Staging does not need strict security
ssh_permit_root_login: "yes"
ssh_password_auth: "yes"

# Shorter log retention
log_retention_days: 7

# Debug mode
app_debug: true
