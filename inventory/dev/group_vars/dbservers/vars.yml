# group_vars/dbservers/vars.yml
# Variable naming: use the database__ prefix consistently, matching roles/database/defaults

---
# ── MySQL config (overrides roles/database/defaults/main.yml) ───────────────
database__mysql_version: "8.0"
database__mysql_port: 3306
database__mysql_bind_address: "0.0.0.0"
database__mysql_datadir: /var/lib/mysql

# ── Performance tuning (overrides role defaults) ────────────────────────────
database__mysql_innodb_buffer_pool_size: "2G"
database__mysql_innodb_log_file_size: "256M"
database__mysql_max_connections: 300

# ── Database list (consumed by roles/database/tasks/configure.yml) ──────────
database__databases:
  - name: appdb
    encoding: utf8mb4
    collation: utf8mb4_unicode_ci
  - name: analyticsdb
    encoding: utf8mb4
    collation: utf8mb4_unicode_ci

# ── Users ───────────────────────────────────────────────────────────────────
database__users:
  - name: appuser
    password: "{{ vault_db_app_password }}"
    priv: "appdb.*:ALL"
    host: "10.0.1.%"
  - name: readonly
    password: "{{ vault_db_app_password }}"
    priv: "appdb.*:SELECT"
    host: "%"

# ── Backup ──────────────────────────────────────────────────────────────────
database__backup_enabled: true
database__backup_dir: /backups/mysql
database__backup_retention_days: 7

# ── Shared variables ────────────────────────────────────────────────────────
firewall_allowed_tcp_ports:
  - 22
  - 3306
