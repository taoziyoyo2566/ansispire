# group_vars/webservers/vars.yml
# Variable naming: use the webserver__ prefix consistently, matching roles/webserver/defaults.
# This guarantees group_vars actually override role defaults.

---
# ── Nginx config (overrides defaults in roles/webserver/defaults/main.yml) ──
webserver__nginx_version: "1.24"
webserver__worker_processes: auto
webserver__worker_connections: 1024
webserver__keepalive_timeout: 65
webserver__client_max_body_size: "64m"

# ── Virtual hosts (consumed by the loop in roles/webserver/tasks/vhosts.yml) ──
webserver__vhosts:
  - name: app.example.com
    root: /var/www/app
    ssl: true
    ssl_cert: /etc/ssl/certs/app.example.com.crt
    ssl_key: /etc/ssl/private/app.example.com.key
    php_fpm: true
    php_fpm_socket: /run/php/php8.2-fpm.sock

  - name: static.example.com
    root: /var/www/static
    ssl: true
    ssl_cert: /etc/ssl/certs/static.example.com.crt
    ssl_key: /etc/ssl/private/static.example.com.key
    php_fpm: false

# ── Shared variables (no role prefix; cross-role use) ───────────────────────
firewall_allowed_tcp_ports:
  - 22
  - 80
  - 443
