---
galaxy_info:
  author: platform-team
  description: Common baseline configuration for all servers
  company: Example Corp
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    # Tier 1 — CI-verified
    - name: Ubuntu
      versions:
        - focal      # 20.04
        - jammy      # 22.04
        - noble      # 24.04
    # Tier 2 — code skeleton present, expected to work
    - name: Debian
      versions:
        - bullseye   # 11
        - bookworm   # 12
    # Tier 1 — validated by the molecule/common Rocky 9 scenario
    - name: EL
      versions:
        - "9"        # Rocky Linux 9 / AlmaLinux 9 (Rocky validated, Alma expected-compatible)
  galaxy_tags:
    - base
    - common
    - security
    - hardening

# Declare dependencies on other roles (they run automatically before this role).
dependencies: []
