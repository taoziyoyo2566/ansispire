# Fleet SSH private keys for onboard managed-channel validation.
# NEVER commit key material. This directory is bind-mounted read-only into the
# Semaphore container at /etc/ansispire/keys. Only this .gitignore and README.md
# are tracked; every actual key file is ignored.
*
!.gitignore
!README.md
