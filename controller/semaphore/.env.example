# Semaphore control plane — sample environment variables
# Usage:
#   cp .env.example .env
#   vim .env       # at minimum set SEMAPHORE_ADMIN_PASSWORD
#   make controller-up

# ── Access ─────────────────────────────────────────────────────────────
# Web UI listen port (host side)
SEMAPHORE_PORT=3000

# If accessed behind a reverse proxy (e.g. under /semaphore/), set WEB_ROOT.
# Leave empty when accessed directly as http://localhost:3000.
SEMAPHORE_WEB_ROOT=

# ── Admin account (created on first boot) ──────────────────────────────
SEMAPHORE_ADMIN=admin
SEMAPHORE_ADMIN_NAME=Administrator
SEMAPHORE_ADMIN_EMAIL=admin@localhost

# ! Must be changed to a strong password; do NOT commit to git
#   (.env is already in .gitignore)
SEMAPHORE_ADMIN_PASSWORD=changeme_please

# ── Timezone ───────────────────────────────────────────────────────────
TZ=Asia/Shanghai
