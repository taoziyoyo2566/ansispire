# molecule/webserver/converge.yml — playbook executed during the test
# Test variables (webserver__vhosts, etc.) come from molecule.yml group_vars.

---
- name: Converge
  hosts: all
  become: true
  gather_facts: true

  roles:
    - role: common
    - role: webserver
