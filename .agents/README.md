# `.agents` Directory

Modular agent guidance for `ansispire`.

- `project/`: repo facts and routing
- `rules/`: stable constraints
- `scenarios/`: task-type flow

These are context modules loaded by `AGENTS.md`, not sub-agents.
