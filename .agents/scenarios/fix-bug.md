# Scenario: Fix Bug

1. Identify the failing behavior and the affected surface.
2. Check whether an investigation, changelog, or operational-truth doc already explains the failure mode.
3. Fix the root cause, not only the symptom, if the repo's current architecture supports it.
4. Add or run the narrowest test that proves the bug is fixed.
5. Sync docs if user-visible behavior changed.
