# Round 1 — 2026-07-18 — saberu-ops/saberu v0.0.1 migration executed

**Plan**: `execution-saberu-0.0.1-repo-migration-2026-07-18.md` (APPROVED 2026-07-18; defaults confirmed: private repo, host gitconfig identity, LICENSE untouched).

## Outcome

- New repository **`saberu-ops/saberu`** (private, default branch `master`) is live.
- Import commit `21501ae` on `feat/saberu-0.0.1-import` carries the pruned
  424-file tree; annotated tag **`v0.0.1`** points at it.
- `master` and `dev` hold only the content-free bootstrap commit `9ac1e06`.
- Fresh-clone verification: tracked file list **identical** to the
  `feat/saberu-migration` tree in this repo; `check_claude_md_links.sh` exit 0;
  detect-secrets gate clean (18 known findings / 400 files).

## Commit manifest (this repo, `feat/saberu-migration`)

| Commit | Change |
|---|---|
| `bbf5050` | Phase 1 — consolidate working tree (45 files, +2339/−241) |
| `8f58146` | Phase 2.1 — prune 103 files (_archive 40, round changelogs 48, reference-cn 11, plugins stub, GEMINI.md, .geminiignore, package.list) |
| `6c948ee` | Phase 2.2 — remove dangling references in 16 live docs/configs |
| `593f644` | Phase 2.4 — TODO ledger trim + migration workstream entry |
| `4fb4040` | Phase 2.5 — CHANGELOG reset to [0.0.1] |
| `eb66510` | Phase 2.6–2.7 — README Saberu identity header + blueprint override note |
| `eb24d7c` | Phase 2.8 — regenerate .secrets.baseline |
| `14cd4a4` | security contact → sp@saberu.com (user decision) |
| `1c2f425` | gitignore negations for force-tracked vendored files (incident fix, see below) |

## Verification (3-gate)

- **Gate 1 (direction)**: prune counts matched the approved list exactly (103 files); file-list diff old-repo↔clone empty.
- **Gate 2 (static)**: `ansible-lint` production profile — 0 failures / 0 warnings (124 files); yamllint via `make lint`; vps_task schema 7/7 valid.
- **Gate 3 (functional)**: playbook `--syntax-check` OK; `check_claude_md_links.sh` OK; detect-secrets gate clean; EDA + filter test suites OK.
- **Blocked (not passed)**: `make verify` final step `dry-run` — this host's sudo requires a password (become failure). Environment constraint, freshly probed 2026-07-18; no playbook/role content changed this round.

## Deviations from plan (all reported in-session)

1. **`taoziyoyo` reference fixes not applied** to the 3 surviving files: SECURITY.md held the user's real contact email (later changed to `sp@saberu.com` by user decision), feature-map INDEX documents the *live* deployed Worker hostname, and the refactor-docs-enterprise plan is historical evidence. The repo-URL references the plan targeted were all inside pruned files.
2. **PR flow dropped** (Phase 4 steps 5): the `saberu-ops` fine-grained PAT lacks Pull-requests permission; user decided the import stays on `feat/saberu-0.0.1-import` with **no merges**, and `v0.0.1` is tagged on the import commit instead of a `master` merge commit.
3. **Export-loss incident, root-caused and fixed**: the first pushed import contained 400 files — a fresh `git add` dropped 24 tracked-but-gitignore-matched files (vendored `geerlingguy.docker` role + `controller/audit/e2e/.env.example`). Fixed by `.gitignore` negations (`1c2f425`), amended import commit, lease-guarded force-push (`--force-with-lease=<ref>:<oid>`, oids captured) and tag re-push; final clone re-verified identical.
4. `docs/workstreams/feat-vps-profile-catalog/_history/` is an empty placeholder directory — git does not track it, so it does not travel; the pending topic-absorption it marks is unaffected.

## NOT done (deferred)

- Internal `ansispire` → `saberu` rename (~191 files) — 0.0.2, L2.
- Merging `feat/saberu-0.0.1-import` into `dev`/`master` — pending user decision + PAT Pull-requests permission (or web-UI merge).
- Old repo archive/private switch; `/tmp` worktree cleanup; reconciliation of the superseded `feat/vps-profile-catalog` / `feat/governance-artifact-routing` tips.
- Live Worker `ansispire-vps-worker.taoziyoyo.workers.dev` still runs under the old account (feature-map INDEX documents current truth).

## Sync notes

- This closeout (changelog + plan/README/TODO status) is committed here and
  synced as a docs commit onto the new repo's import branch so both repos read
  the same final state. The `v0.0.1` tag content itself is unchanged.
