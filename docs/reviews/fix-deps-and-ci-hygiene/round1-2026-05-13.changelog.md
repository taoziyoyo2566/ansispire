# Round 1 Changelog — Fix Deps & CI Hygiene (2026-05-13)

Reference: [plan-2026-05-13.md](plan-2026-05-13.md)

Branch: `dev` (6 commits, not pushed; merge-to-dev is user-owned).

## File Change Manifest

| File | Change | Summary |
|---|---|---|
| `molecule/default/molecule.yml` | added | Stub default scenario silences Molecule 26+ shared-state probe (`CRITICAL ... glob failed.`). |
| `docs/reference/feature-map/test-infra.md` | modified §4 | CI section: 3-job flat list → 6-job DAG diagram matching actual `needs:` topology. |
| `docs/reference/feature-map/INDEX.md` | modified §7 | One-line CI summary updated for the same DAG. |
| `.github/dependabot.yml` | modified | Added `target-branch: dev` to both ecosystems; tightened header comment. |
| `requirements.txt` | modified | `ansible-core==2.20.5` → `>=2.20.5` (minimums, not pins). |
| `pyproject.toml` | modified | `ansible-core>=2.16,<2.18` → `>=2.20.5`; build-backend typo (`hatchling.backends` → `hatchling.build`) + `bypass-selection = true` for empty-wheel; floors aligned with requirements.txt (ansible-lint, jmespath, molecule, molecule-plugins). |

## Intent Per Change

| Commit | Intent |
|---|---|
| `d92e30a chore(molecule): add stub default scenario to silence shared-state probe` | Eliminate misleading CRITICAL log; do NOT enable shared-state (verified per-scenario opt-in in molecule source). |
| `c55dfe9 docs(feature-map): sync CI section to actual ci.yml (6 jobs)` | Doc-truth ↔ code-truth sync; future agents read the doc and trust it. |
| `bc62e58 chore(dependabot): target dev branch (development-phase trunk)` | Align tooling with the project's actual branch model. Existing PRs against `master` are stale-base; new PRs will land on `dev` directly. |
| `4974e4c chore(deps): align ansible-core constraint with version policy` | Apply "minimums, not pins" + "default latest stable" to the only outlier (`==2.20.5`) and to a stale pyproject cap (`<2.18` excluded 2.20.5). |
| `314358e fix(pyproject): make pip install -e . actually work` | Restore the install path documented in the file's own header comment; reveal that the project ships no Python package source (deps-only manifest). |
| `3b73ffb chore(pyproject): align overlapping floors with requirements.txt SSOT` | Eliminate floor drift on ansible-lint / jmespath / molecule / molecule-plugins between the two manifests. |

## NOT Done (Explicit)

- Did not push the 6 commits — user-owned per `feedback_branching_model.md`.
- Did not close any of the 6 OPEN Dependabot PRs against `master` — recommendation only; user owns the PR-level actions.
- Did not double-list `requirements.txt`-only deps (cryptography, requests, PyYAML) into `pyproject.toml`. These are transitive via ansible-core; explicit listing is a separate decision.
- Did not bump pyproject-only deps' floors (yamllint, docker, pytest, ansible-navigator) — they have no SSOT counterpart to align with.
- Did not touch other parts of the project beyond the scope above (no role refactors, no playbook changes, no controller code).
- Did not add a `CHANGELOG.md [Unreleased]` entry: re-reading the trigger criteria (CLI behavior / config defaults / public interface / breaking refactor / security policy), none of the six commits qualify. `pip install -e .` is a developer install path, not user-facing CLI; deps floor changes are not "configuration defaults" in the `defaults/` / `group_vars/` / role-knob sense.

## Self-Check Results (W-R15 3-gate × 6 commits)

All gates passed in the **same iteration** (W-R17 cap not reached — `314358e` did need a second iteration to add `bypass-selection`, see commit message).

End-to-end test matrix:

| Test | Scope | Result |
|---|---|---|
| `yamllint .` (tracked files only, CI parity) | repo | ✅ exit 0 |
| `dependabot.yml` structural validation | both ecosystems have `target-branch: dev` | ✅ |
| `pyproject.toml` TOML parse | dependencies + build-system + hatch config | ✅ |
| feature-map docs ↔ ci.yml DAG cross-check | 6 jobs, `needs:` topology | ✅ identical |
| `pip install -r requirements.txt` (fresh venv) | ansible-core 2.20.5 + molecule 26.4.0 + cryptography 48.0.0 pulled | ✅ |
| `pip install -e ".[test]"` `--dry-run` | hatchling builds empty wheel; ansispire-0.1.0 resolves | ✅ |
| `molecule list` | 5 scenarios load (4 real + default stub) | ✅ |
| `molecule syntax -s <X>` × {common, webserver, database, full-stack} | stub doesn't break real scenarios | ✅ 4/4 |
| `ansible-playbook --syntax-check` × {stag, prod} | playbooks parse against both inventories | ✅ |
| `ansible-lint --profile production` | CI parity | ✅ exit 0 |
| ansible-vault encrypt + decrypt round-trip on cryptography 48.0.0 | AES256 path under target Dependabot floor | ✅ original recovered byte-for-byte |
| `ansible-vault encrypt_string` on cryptography 48.0.0 | `!vault` YAML scalar generation | ✅ |
| Memory file frontmatter validity | 2 new/modified + MEMORY.md index | ✅ |

## CLAUDE.md / Memory Updates (This Round)

Auto-memory only — no project / workspace / user CLAUDE.md changes (none of
the new patterns met the W-R14 universality test for resident context):

- `~/.claude/projects/.../memory/feedback_branching_model.md` — updated to
  add "development-phase: dev IS the trunk; master is held for post-launch".
  Triggered by user clarification 2026-05-13.
- `~/.claude/projects/.../memory/feedback_version_policy.md` — new file:
  requirements declares minimums (>=), not pins; default latest stable;
  Docker image SSOT lives in `config/manifest.yml`. Triggered by two user
  clarifications 2026-05-13.
- `~/.claude/projects/.../memory/MEMORY.md` — index updated to reflect the
  branching-model rewording and to add the version-policy entry.

## Process Note

This round violated workspace W-R "plan-first" (no plan doc existed before
implementation began). The work emerged from a user question about CI log
warnings and extended through five "请继续" iterations. The plan doc above
is retroactive. The trigger for retroactive process repair was the user
sending another "请继续" after I had declared the round closed.

The class of work (small, organic, dialog-driven hygiene) is plausibly a
case where the plan-first ceremony adds little — but workspace policy is
clear that the artifacts must exist regardless. Recording this here so the
pattern is visible if it repeats.

## Follow-Up / Next Steps

**Immediately doable (user-side)**:

1. Close the 6 OPEN Dependabot PRs against `master`. Either let next Monday's
   cycle regenerate them against `dev`, or comment `@dependabot recreate` on
   each to accelerate.
2. Merge the 6 local commits via the existing merge-to-dev workflow.

**Blocked**: none.

**Deferrable / removable**:

- Whether to double-list cryptography / requests / PyYAML in `pyproject.toml`
  (currently only in `requirements.txt`). Pure decision question.
- Whether to bump `requires-python` from `>=3.11` to `>=3.12` (CI tests only
  3.12, not 3.11). Honesty-vs-flexibility tradeoff.
- After Dependabot regenerates PR #11 (`cryptography 42→48`) against `dev`,
  it can merge without additional smoke — the smoke test in this round
  already validated the 48.0.0 AES256 path.
