# Round 4 — Runner Mode Documentation Alignment

**Date**: 2026-05-14
**Reference**: [plan-2026-05-13.md](plan-2026-05-13.md)
**Level**: [L1] Engineering — doc / runner metadata alignment

---

## 1. File change manifest

| File | Change | Summary |
|---|---|---|
| `scripts/loopback_test_runner.sh` | edit | Clarified usage text and phase comment for `exhaustive`; allowlisted the disposable smoke password placeholder for detect-secrets. |
| `docs/governance/loopback-runner.md` | edit | Aligned mode table, output layout, preflight notes, phase table, and troubleshooting with actual `quick` / `standard` / `ci-equiv` / `full` / `exhaustive` behavior. |
| `docs/governance/testing-governance.md` | edit | Added `exhaustive` to structured runner references and corrected the CI dependency graph to match the workflow. |
| `README.md` | edit | Synced runner timing comments with the corrected mode semantics. |

## 2. Review findings fixed

| Finding | Resolution |
|---|---|
| `loopback-runner.md` described `ci-equiv` as including `full` + e2e, but the script runs `ci-equiv` as standard + molecule only. | Document now maps `ci-equiv` to the local CI mirror, `full` to isolated smoke, and `exhaustive` to the extra disposable e2e stack. |
| Output layout listed stale smoke logs (`audit-up.log`) and wrong mode comments. | Output tree now lists `wait.log` / `audit-reload.log`, marks molecule as `ci-equiv`+ and smoke as `full`+. |
| The design section overstated that every gate goes through Make. | Document now distinguishes Make-backed gates from direct coverage / molecule / isolated smoke invocations. |
| `testing-governance.md` omitted `exhaustive` and showed `detect-secrets` as part of the molecule dependency chain. | Runner references include all five modes; CI graph now shows secret detection as an independent merge gate. |
| `detect-secrets` flagged the runner's hard-coded disposable smoke password placeholder. | Added an explicit `pragma: allowlist secret` to document the intentional test-only value. |

## 3. Validation

| Command | Result | Notes |
|---|---|---|
| `bash -n scripts/loopback_test_runner.sh` | PASS | shell syntax clean |
| `python3 -m py_compile scripts/detect_secrets_gate.py` | PASS | Python syntax clean |
| `make test-eda test-filters` | PASS | 42 unit / component cases total |
| `make detect-secrets` | PASS | 18 known findings, 305 files scanned |
| `SKIP_BOOTSTRAP=1 ./scripts/loopback_test_runner.sh quick` | PASS | syntax-only runner gate |
| `SKIP_BOOTSTRAP=1 ./scripts/loopback_test_runner.sh standard` | PASS | 13/13 steps green, coverage threshold passed |
| `make verify` | PASS | fail-fast push gate green |

## 4. Remaining notes

- `ci-equiv`, `full`, and `exhaustive` were not rerun in this round; this pass changed docs / usage text only, plus a detect-secrets allowlist comment.
- The latest structured evidence is under `test_results/latest/` and is ignored by git.
