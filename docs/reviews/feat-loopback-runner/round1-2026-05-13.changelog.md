# Round 1 — Loopback Test Runner v2.0

**Date**: 2026-05-13
**Reference**: [plan-2026-05-13.md](plan-2026-05-13.md)
**Level**: [L1] Engineering

---

## 1. File change manifest

| File | Change | Summary |
|---|---|---|
| `scripts/loopback_test_runner.sh` | rewrite | v1 → v2.0. Thin orchestrator over `make` targets; fail-collect; trap-driven teardown; isolation dir via `mktemp`; history retention with `latest` symlink; real `coverage --fail-under` threshold. ~440 lines. |
| `docs/governance/loopback-runner.md` | new | User-facing spec: mode table, output layout, env-var reference, exit codes, design properties, preflight gates, phase-to-make mapping, troubleshooting, CI parity statement. |
| `docs/governance/testing-governance.md` | edit | Added "结构化跑" pointer to the Quick reference at top; added §5 sub-table contrasting `make` direct vs. runner-driven gate execution. |
| `docs/reviews/feat-loopback-runner/plan-2026-05-13.md` | new | Plan doc per W-R1/W-R10. |
| `docs/reviews/feat-loopback-runner/round1-2026-05-13.changelog.md` | new | This file. |

**Not modified** (left for follow-up commits):
- `.yamllint` — Gemini's runtime-dir ignore additions stay; should be a separate `chore(yamllint)` commit.
- `controller/rbac/smoke.sh` — Gemini's `expect_code_or` fix stays; should be a separate `fix(rbac)` commit.
- `controller/audit/test_filters.py`, `test_relay.py`, `test_sink.py` — orphan tests; the runner picks them up as "extras" but they're not yet in any make target. To register or delete in F2 (see plan §5).
- `Makefile` — no changes.

## 2. Intent

| Change | Intent |
|---|---|
| Script rewrite | Replace v1's two correctness bugs (working-tree credential overwrite, 100%-coverage false positive) + structural drift from testing-governance §3-§5 with a hardened, reusable, history-retaining runner. |
| `docs/governance/loopback-runner.md` | Resident spec contributors can read in 5 min to know which mode to run when, what files appear under `test_results/`, what each env var does. Companion to (not replacement for) `make verify*`. |
| `testing-governance.md` cross-reference | Make the runner discoverable from the canonical testing entry point; otherwise contributors who know the make-target story won't find the runner. |

## 3. NOT done (explicit)

- `full` and `ci-equiv` modes were NOT executed end-to-end in this round (would require ~15-25 min each + clean Semaphore state). The phases are present in the script and structurally mirror the validated `standard` path; high confidence but not directly proven by gate-3 execution.
- F1–F4 follow-ups in plan §5 are NOT executed.
- `CHANGELOG.md [Unreleased]` not updated this round (deferred to closeout when the related PR opens).
- `ARCHITECTURE.md` not updated (the runner is a tool, not an architectural component).
- No TSVS written for the new functionality — the runner itself isn't a "functional test"; it's a test-runner. TSVS rule (§7) applies to test additions, not orchestrator changes.

## 4. Self-check results (per W-R15 3-gate)

**Iteration 1**: gates 1 → 2 → 3 → fix → restart from gate 1.

| Gate | Result | Notes |
|---|---|---|
| **Gate 1 — Direction** | PASS | Diff matches user directive ("完善的测试脚本 + 说明文档") and plan §2 scope. No scope creep into Makefile / .yamllint / smoke.sh. |
| **Gate 2 — Syntax / static** | PASS | `bash -n scripts/loopback_test_runner.sh` clean. `shellcheck` not installed on host (not blocking). Markdown docs render in preview without syntax errors. |
| **Gate 3 — Functional** | FAIL (then fixed) | First `quick` run revealed `count_failures` returning `"0\n0"` due to `grep -c \|\| echo 0` double-output; arithmetic `[ -gt 0 ]` failed in trap. |

**Iteration 2** (after fix replacing `grep -c \|\| echo 0` with `awk '... END{print n+0}'`):

| Gate | Result |
|---|---|
| **Gate 1 — Direction** | PASS |
| **Gate 2 — Syntax** | PASS |
| **Gate 3 — Functional** | PASS — `quick` mode: 2/2 PASS, 2s. `standard` mode: 12/12 PASS, 55s. Coverage 87% TOTAL, real `--fail-under=70` triggered. `SUMMARY.md`, `latest` symlink, html coverage drilldown all produced. |

All three gates clean in the same iteration. Done.

## 5. CLAUDE.md updates (this round)

None. The work is engineering-level and falls under existing rules (W-R1 plan-first, W-R10 doc-naming, W-R13 credentials-isolation, W-R15 3-gate). No new cross-project patterns surfaced.

## 6. Sync-update (Peer Rule §0)

| Truth source | Touched? | Synced? |
|---|---|---|
| `ARCHITECTURE.md` | No (runner is a tool, not a subsystem) | N/A |
| `README.md` | No | N/A (could add a one-liner in F1+ commit hygiene round) |
| `docs/reference/feature-map/<name>.md` | No | N/A (no feature map entry for test infrastructure currently) |
| `CHANGELOG.md [Unreleased]` | No | Deferred to PR opening |
| `docs/reference/feature-map/INDEX.md` | No (no roles/playbooks/controller code changes) | N/A |

## 7. Follow-Up / Next Steps

**Immediately doable**
- [Claude] If you want, run `./scripts/loopback_test_runner.sh full` on this host to validate molecule + L5 smoke chain (~15 min); will surface any environmental issue before you depend on it.
- [Claude] Split out Gemini's two legitimate but-unrelated edits into separate commits (F1): `chore(yamllint): ignore runtime dirs` and `fix(rbac): consolidate dual-expectation HTTP code check`.
- [you] Decide on the three new orphan test files: register in `make test-eda-*` + write TSVS, or delete. If keeping, F2 round.
- [you] Decide whether the runner's defaults (`COVERAGE_MIN=70`, `MOLECULE_PARALLEL=0`, `LOOPBACK_HISTORY_KEEP=10`) match your intent — all are env-overridable; defaults are conservative.

**Blocked**
- F3 (detect-secrets local) needs `.secrets.baseline` decision: generate a fresh baseline, or import from CI config.
- F4 (full / ci-equiv validation) is blocked on a clean machine state (no leftover containers from manual work). ~25 min of dedicated bandwidth.

**Deferrable**
- Adding a `--clean-history` flag / a `make verify-loopback` target wrapper — only if usage patterns prove it helpful.
- Streaming phase markers (W-R16) — currently the runner already logs every step inline so foreground use is informative; only useful if you ever background it.
