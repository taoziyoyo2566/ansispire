# refactor-env-registry-hoist — Round 1: env registry hoisted to workspace layer

**Date**: 2026-07-11
**Branch**: `refactor/env-registry-hoist` (stacked on `feat/target-architecture`)
**Level**: 🔴 [L2] Architecture (cross-project) — realizes IVG-TOOLENV-REGISTRY §4.2's three-layer model.

**Reference**: [`plan-hoist-env-registry-to-workspace-meta-2026-07-11.md`](./plan-hoist-env-registry-to-workspace-meta-2026-07-11.md) (APPROVED) · IVG-TOOLENV-REGISTRY (OQ-1 resolved) · workspace-meta `115dfb6` / `437d8d0`

---

## Scope

The per-host env-capability registry was duplicated inside every product repo. Hoist the portable mechanism + host data to the workspace layer so all `~/workspace` projects share one probe; leave only the ansispire override tail here.

---

## Landed (across 3 locations)

| Location | Commit | Change |
|---|---|---|
| workspace-meta (`~/workspace`) | `115dfb6` | `scripts/env_probe.sh` (generic; `running_count` not names; atomic write), `Makefile` env-probe[-check], `.agents/rules/environment-truth.md`, `.agents/env/README.md` + `mail.yml`, `.gitignore` whitelist. **Pushed to origin/main.** |
| workspace-meta | `437d8d0` | Landed the stranded **W-R27** (§5 + register) — authored 2026-06-26, never committed (15d W-R26 drift). **Pushed.** |
| host `~/.claude/` | (not git) | `skills/env-sync/SKILL.md` (genericized) + SessionStart env-probe-check hook merged into `settings.json` (preserved the existing workspace-meta-freshness hook). |
| ansispire | `e9b68f8` | Removed the moved files; `Makefile` shims delegate to `~/workspace`; `.agents/env/README.md` rewritten as the project override layer; re-pointed session-bootstrap / coding-plan / evidence-backed-planning / execution-reflection / feature-map INDEX / investigations INDEX / IVG; synced CHANGELOG + TODO. |

---

## Verification (3-gate)

| Gate | Result |
|---|---|
| workspace-meta probe | `bash -n` clean; `make -C ~/workspace env-probe` writes `mail.yml`; `--check` fresh; atomic write leaves no temp; pre-commit whitelist guard passed on commit. |
| host hook | pipe-test FRESH → silent; STALE (`ENV_PROBE_TTL_DAYS=0`) → valid JSON `systemMessage` pointing at `make -C ~/workspace env-probe`. |
| ansispire shim | `make env-probe-check` delegates to `~/workspace` and passes; `settings.json` valid JSON; no dangling active reference to removed paths. |

---

## Review findings folded in (2026-07-11)

- **W-R27 stranding** (workspace-meta): landed as its own commit — the highest-value finding.
- **Project-venv tool blindness** (🟡): the bare-host probe now reports ansible/lint absent; added the ansispire project override layer (`.agents/env/README.md`) so the host-level `false` is not misread as "cannot lint here."
- **`git_push` semantics**: documented that it now probes workspace-meta's origin as a host git-reachability proxy.
- **Minors**: atomic registry write; generic host_override wording; README forward-ref softened.

---

## Not done / deliberate choices

- **`saberu.yml` dropped, not migrated.** The other host's 2026-06-10 snapshot was past-TTL; per environment-truth it must be re-probed anyway, so it regenerates into workspace-meta when that machine next runs `make env-probe`. Recoverable from ansispire git history if continuity is wanted.
- Historical narrative in older IVG sections + `docs/reviews/` plan files still names the old in-repo paths — intentional (they describe the pre-hoist state).
- `.claude/settings.local.json` (gitignored, local) retains stale allow-patterns for the removed script paths — harmless unused permission grants.

---

## Next Steps

| Status | Action |
|---|---|
| **Ready** | Push `refactor/env-registry-hoist` and open PR → `feat/target-architecture` (its parent per W-R21 §b). |
| **Ready** | Resume the Saberu main line — **Phase 2 onboard runtime contract** (independent of this refactor). |
| **Optional** | Migrate `saberu.yml` to workspace-meta if cross-host snapshot continuity is wanted. |
