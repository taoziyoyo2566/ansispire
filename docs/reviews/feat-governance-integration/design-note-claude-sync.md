# 技术调查报告 (Technical Investigation Report) - Gemini 规则同步至 Claude

## 1. 调查概览 (Overview)
- **调查 ID**: `IVG-TASK-CLAUDE-SYNC`
- **关联任务**: 同步 Gemini 治理规则至 Claude 环境
- **调查类型**: 架构同步 / 最佳实践调研
- **目标**: 调研如何将 Ansispire 的 AI-Native 规则 (GEMINI.md) 转化为适用于 Claude 的最佳实践。

## 2. 背景与问题描述 (Background)
- **初始观察**: 项目目前拥有完善的 `GEMINI.md` 规则，但 `CLAUDE.md` 相对独立，且未充分利用 Claude 的 XML 标签、Prompt Caching 和系统提示词特性。
- **影响范围**: 跨 AI 协作的一致性，以及在 Claude 环境下的执行效率。

## 3. 调查过程 (Investigation Process)
### 3.1 核心发现 (Key Findings)
1. **XML 标签增强**: Claude 极其擅长识别 XML 标签。应将治理规则、任务状态和证据块包裹在 `<instructions>` 或 `<evidence>` 标签中。
2. **Prompt Caching 优化**: 既然 Ansispire 使用分层加载，应建议在 Claude 环境中将 `SUMMARY.md` 等核心文档放入 Cache，而将 `investigations/` 设为按需加载。
3. **思维链 (Thinking)**: Claude 的 `Thinking` 模式适用于复杂的架构决策，应在 `CLAUDE.md` 中明确要求在执行 [L2] 任务时启用思考过程。

### 3.2 规则调整建议 (Proposed Adjustments)
- **分层加载**: 借鉴 `GEMINI.md` 的三层真相。
- **XML 规范**: 在 `CLAUDE.md` 中强制使用 XML 标签记录 Changelog 和验证。

## 4. 证据与日志 (Evidence & Logs)
> 参考 Anthropic 官方文档：
> - 使用 XML 标签来组织 Prompt 是 Claude 的核心实践。
> - Prompt Caching 可显著降低大型代码库的交互延迟。

## 5. 发现与分析 (Findings)
- **一致性**: 现有的 `GEMINI.md` 逻辑（控制平面/数据平面分离、分层文档治理）完全适用于 Claude。
- **性能**: 通过调整 `CLAUDE.md` 的结构，可以提高 Claude 在分析复杂 Ansible 角色时的准确度。

## 6. 结论与建议 (Conclusion)
- **行动方案**: 重写 `CLAUDE.md`，引入 XML 标签支持，并同步 `GEMINI.md` 中的调查协议 (Investigation Protocol)。
- **修复方案**: 将 `GEMINI.md` 中的治理哲学作为 `CLAUDE.md` 的核心，但采用 Claude 偏好的 XML 格式。

## 7. 关联验证 (Linked Verification)
- **TSVS 引用**: N/A
- **验证结果**: 待执行 `CLAUDE.md` 更新。

---
*Generated by Ansispire Investigation Engine*
