# feat-target-architecture — Round 13: branch-owned architecture hub + documentation truth reconciliation

**Date**: 2026-07-12
**Branch**: `feat/target-architecture`
**Level**: Documentation / architecture evidence reconciliation
**Reference**: [`round12-2026-07-11.changelog.md`](./round12-2026-07-11.changelog.md) + repository-wide review requested on 2026-07-12

## Scope

1. Keep the target/as-built diagram set with the branch deliverables that created it.
2. Restore one reader-facing feature hub instead of competing `README` / `README2` files.
3. Reconcile active operator, feature-map, test-spec, and architecture claims with the current implementation and recorded evidence.
4. Preserve historical plans/changelogs; do not rewrite earlier round evidence.

## Decisions

- **Feature-owned package**: `docs/feat-target-architecture/` owns the stable reader-facing output; `docs/reviews/feat-target-architecture/` remains dated process evidence.
- **TARGET and AS-BUILT stay adjacent**: both live under `diagrams/`, with AS-BUILT nested under `diagrams/as-built/`. Splitting TARGET and AS-BUILT across unrelated roots is not supported.
- **Evidence labels are explicit**: real-VPS proof, Docker E2E, unit/component coverage, and unverified target behavior are not interchangeable.
- **Security wording is bounded**: the JSONL sink is an application-level append writer, not cryptographic non-repudiation or WORM storage; relay backfill is cursor-based but bounded.

## Landed

| Surface | Change |
|---|---|
| `docs/feat-target-architecture/` | Restored the feature hub README, kept the Semaphore-native operator guide, and consolidated TARGET/AS-BUILT PlantUML + SVG under `diagrams/`. |
| Diagram facts | Updated Reactor v2.3 → v2.6, corrected Semaphore Environment injection, separated Debian UFW from RHEL EPEL/fail2ban, and labelled E2E coverage boundaries. |
| VPS operator docs | Corrected the manual runbook to use one fleet key for bootstrap transport and managed validation; routed the active path through Semaphore UI and `vps-fleet`. |
| Worker docs | Marked Worker onboarding deferred and incompatible with the current transport-key reuse contract; checked-in template IDs remain empty. No Worker runtime code changed. |
| Audit/EDA docs | Removed unsupported zero-loss/non-repudiation claims, split `Semaphore → relay → sink` coverage from `sink → reactor → remediation`, and synchronized the current 46-test EDA suite. |
| Project truth | Updated `ARCHITECTURE.md`, README/document maps, TODO, feature maps, operations docs, TSVS index/specs, and `[Unreleased]` status. |

## Deliberately not changed

- No runtime code, Semaphore resources, Worker deployment, inventory, or VPS state changed.
- Worker `onboard-bare` remains disabled/deferred; its code/tests still require a future contract update.
- Relay's 500-event-per-poll backfill cap remains implementation truth; docs no longer call it an unbounded zero-loss guarantee.
- RHEL onboarding was not rerun; EPEL reachability is the first observed blocker and later RHEL steps remain unverified.
- Unrelated untracked `package.list` was preserved and excluded from this round.

## Verification

| Check | Result |
|---|---|
| PlantUML 1.2024.7 `-checkonly` on all six sources | PASS |
| Regenerate all six committed SVGs from their `.puml` source | PASS |
| Local relative-link scan for every changed Markdown file | PASS |
| `make vps-lifecycle-syntax` | PASS for all six playbooks; existing `apt_repository` deprecation warning only |
| `make test-eda` | PASS: 19 reactor + 11 contract + 5 component + 6 relay + 5 sink; rules schema PASS |
| `make detect-secrets` | PASS: clean against baseline |
| `git diff --check` | PASS |

## Closeout

The documentation now has one coherent feature-owned entry point, one adjacent
TARGET/AS-BUILT comparison, and current operational claims tied to the validation
carrier that actually proves them. Remaining gaps are implementation or live-host
work, not unresolved documentation ambiguity.
