# feat-target-architecture — Round 4: scope lock + project hygiene

**Date**: 2026-06-05
**Branch**: `feat/target-architecture`
**Level**: 🟡 [L1] Engineering (scope decision reflection + truth-source sync) — no new subsystem.

**Reference**: [`plan-2026-05-25.md`](./plan-2026-05-25.md) · [`design-2026-05-26.md`](./design-2026-05-26.md) · [`round3-2026-06-03.changelog.md`](./round3-2026-06-03.changelog.md)

## Scope

User direction (2026-06-05) locked four decisions and authorized cleanup:
1. Drop the `todo` branch mechanism — `TODO.md` is the sole task ledger.
2. Drop Alpine — supported OS set is **Debian + RHEL families only**.
3. `feat/target-architecture` must **never merge to `master`** (feat → `dev` only).
4. Remove the untracked `reference/ansispire/` duplicate.

Plus: "确定未来方向并整理资料" — consolidate the forward roadmap into a coherent TODO.

## Landed

| File | Change | Intent |
|---|---|---|
| *(deleted)* `reference/ansispire/` | remove | 3.6 MB untracked full-repo duplicate; accidental-commit + lint/grep-pollution risk |
| `roles/infra_baseline/tasks/main.yml` | edit | Alpine `NOT IMPLEMENTED` stub → generic unsupported-family guard (`os_family not in [Debian, RedHat]`); header comments de-Alpine'd |
| `inventory/hosts.ini` | edit | drop empty `[targets_alpine]` group + `[targets:children]` ref + taxonomy comment |
| `inventory/prod/hosts.ini` | edit | same |
| `playbooks/deploy_target.yml` | edit | header OS-support comment → Debian+RHEL complete set |
| `config/manifest.yml` | edit | forward-compat comment drop `alpine` pin example |
| `controller/semaphore/bootstrap.yml` | edit | bootstrap summary line drop `targets_alpine empty — TASK-007.B` |
| `ARCHITECTURE.md` / `README.md` | edit | inventory taxonomy + managed-node tiers → complete set, no Alpine |
| `docs/reference/feature-map/{INDEX,multi-os-fleet,hub-deployment,test-infra}.md` | edit | per-feature truth → Alpine out of scope (INDEX sync mandatory per CLAUDE.md §0) |
| `docs/user-guide/{01-installation,04-troubleshooting,05-glossary}.md` | edit | tiers + targets glossary + troubleshooting guard message |
| `docs/operations/{hub-deployment,eda-core}.md` | edit | inventory example + troubleshooting + roadmap line |
| `docs/governance/operational-truths.md` | edit | Tier-2 = RHEL only; "supported set is closed" clause added |
| `CLAUDE.md` | edit | §2 dynamic truth → `TODO.md` only (todo branch retired) |
| `TODO.md` | rewrite | whole-file refactor: Scope-decisions block, remove TASK-007.B, replace 3-week-stale Branch-Readiness with current-branch reality, Open-Decisions table, forward roadmap |
| `CHANGELOG.md` | edit | new `[Unreleased]` subsection (2026-06-05 scope lock); fix TASK-007 entry's Alpine-placeholder line |
| `docs/reviews/feat-target-architecture/TODO-2026-06-03-branch-management.md` | edit | sync stale checkboxes (snapshot commit `3cf02fe` landed; docs committed) |

Git ref ops:
- Created annotated tag `archive/todo-ledger-2026-06-05` at todo tip `1b5189a` (preserves the 3 ledger commits).
- Deleted **local** `todo` branch after SHA-equality verify (W-R19: `-D` only after `rev-parse == expected-oid`).

## NOT done (explicit)

- **Remote git ops blocked on credentials**: this workspace has an HTTPS `origin`, no `gh`, no stored token. The archive-tag **push** and the **remote `todo` branch delete** could not run here — handed to the user (commands in §Follow-Up). Until then `origin/todo` still exists and the archive tag is local-only.
- **No functional (gate-3) ansible run**: `.venv/bin/ansible-playbook` / `ansible-inventory` are absent in this workspace (same blocker recorded in round3). The OS-guard change and inventory edits were not exercised through `ansible-playbook --syntax-check`.
- Did **not** touch vendored `roles/geerlingguy.docker/meta/main.yml` (Alpine in platform list — third-party, out of scope to edit).
- Did **not** rewrite historical IVG docs / archived reviews that mention Alpine as point-in-time context (they are immutable records).

## Self-check (W-R15 3-gate)

- **Gate 1 — direction**: each of the 4 user decisions maps to concrete edits; diff re-read against the decision list. ✅
- **Gate 2 — syntax/static**: `configparser` parse of both `hosts.ini` (confirms `targets_alpine` gone, `[targets:children]` references only defined groups — no forward-ref); `yaml.safe_load` of `manifest.yml`, `bootstrap.yml`, `deploy_target.yml`, `infra_baseline/tasks/main.yml` — all parse. `grep` confirms zero `targets_alpine`/`TASK-007.B` left in `inventory|roles|controller|config|playbooks|Makefile`. ✅
- **Gate 3 — functional**: **NOT run** — ansible binaries absent (see NOT-done). The only behavioural change (OS-family guard) needs an `ansible-playbook --syntax-check playbooks/deploy_target.yml` + a real/Molecule run in an environment with ansible. ⚠️ Recorded as an environment blocker, not a pass.

## CLAUDE.md updates (this round)

- Project `CLAUDE.md §2`: dynamic truth narrowed to `TODO.md` (todo branch retired). No workspace-level (`~/workspace/CLAUDE.md`) rule change.

## Follow-Up / Next Steps

**Immediately doable (you — credentialed shell):**
```bash
# push the archive tag, then race-safe delete the remote todo branch (W-R19 lease)
git push origin archive/todo-ledger-2026-06-05
git push origin --force-with-lease=refs/heads/todo:1b5189ad251dad495c56c2a1269078d81d006e37 :refs/heads/todo
```

**Immediately doable (me, in-repo, no Docker needed):** TASK-009 dependency-security-governance — the one P1 that does not need the blocked runtime.

**Blocked (need environment):** Target Architecture Phase 1 runtime probe — needs a host with Docker daemon + ansible venv (TODO.md Open-Decisions ENV).

**Blocked (need user decision):** Q4 CF Worker language · D1 hub_remote · D2 DB-failover model (TODO.md Open-Decisions table).
