# feat-target-architecture — Round 2 owner-branch cleanup

**Date**: 2026-06-03
**Branch**: `feat/target-architecture`
**Level**: Architecture implementation bootstrap

## Scope

- remove the local `vps_manager` control surface from the owner branch
- retain the VPS lifecycle Ansible content under a Semaphore-first path
- rewrite active docs and agent-routing surfaces so this branch reflects the new truth

## Landed

| File / area | Change | Why |
|---|---|---|
| `plugins/vps_manager/` | removed | this branch should no longer ship a second local control plane |
| `playbooks/vps/` | new retained lifecycle surface | keep the actual Ansible content that the target architecture still needs |
| `Makefile` | removed local plugin entry points; added `vps-lifecycle-syntax` | keep only the branch-valid gate |
| `README.md`, `ARCHITECTURE.md`, `docs/README.md` | branch-truth rewrite | make the repo entry points match the owner branch reality |
| `docs/operations/vps-lifecycle.md`, `docs/reference/feature-map/vps-lifecycle.md` | new active docs | replace `vps_manager` operator / feature-map docs with Semaphore-first lifecycle docs |
| testing governance / test plan / TSVS index | retired old local lifecycle coverage and rewired active gate names | avoid stale “run removed tests” instructions |
| `.agents/`, `playbooks/AGENTS.md`, `plugins/AGENTS.md`, topic-local `AGENTS.md` | routing update | future agent sessions must not drift back to the removed plugin model |

## Boundaries

- did **not** implement Semaphore Inventory / Key Store / Task API wiring yet
- did **not** add CF Worker wizard code yet
- did **not** claim end-to-end VPS lifecycle validation on this branch; current active gate is syntax-only

## Resulting Branch Truth

`feat/target-architecture` is no longer just a planning parent. It is now the owner branch whose checked-out tree already rejects the old local `vps_manager` control surface and keeps only the retained VPS lifecycle playbooks plus the docs needed to continue the Semaphore-first cutover.

## Next Expected Work

1. Phase 1 Semaphore Inventory / Task API investigation.
2. Q4 decision for CF Worker language.
3. Phase 3 follow-up branch for actual Semaphore wiring of `playbooks/vps/`.
