# feat-target-architecture — Round 1 branch bootstrap

**Date**: 2026-06-03
**Branch**: `feat/target-architecture`
**Level**: Architecture planning bootstrap

## Scope

- establish the owner branch for the "return to Ansible + Semaphore" direction
- carry over the agent-governance layer so future work on this branch is agent-routable
- normalize imported target-architecture docs to the current repo truth

## Landed

| File | Change | Why |
|---|---|---|
| `docs/reviews/feat-target-architecture/plan-2026-05-25.md` | owner-branch normalization | stop treating this as cross-branch floating plan |
| `docs/reviews/feat-target-architecture/design-2026-05-26.md` | current-truth normalization | current repo still ships `vps_manager`, so the cutover design must remove that, not a nonexistent current `vps_runner` baseline |
| `docs/reviews/feat-target-architecture/TODO-2026-06-03-branch-management.md` | branch bootstrap status updated | make ownership and next steps explicit |
| `docs/reviews/feat-target-architecture/AGENTS.md` | new | give future agents a local routing / ownership rule set |
| `AGENTS.md`, `.agents/`, nested `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, AI-governance docs | carried onto this branch | future planning and implementation on this branch should be agent-assisted from the start |

## Boundaries

- did **not** remove `vps_manager` code in this round
- did **not** delete unrelated docs in this round
- did **not** rewrite user-facing product docs to pretend the cutover already happened

## Why The Split Exists

`feat/vps-manager-v2` is useful historical evidence, but it is the wrong owner for the accepted direction. The accepted direction is "Semaphore as control-plane truth, Ansispire as content + EDA + wizard layer". That requires its own planning parent branch.

## Next Expected Work

1. Phase 1 Semaphore Inventory / Task API investigation.
2. Q4 decision for CF Worker language.
3. Later cutover branch for actual `vps_manager` removal and doc cleanup.
