# feat-target-architecture — Round 3 Phase 1 contract investigation

**Date**: 2026-06-03
**Branch**: `feat/target-architecture`
**Level**: Architecture investigation / design correction

## Scope

- deliver the Phase 1 investigation artifact for Semaphore Inventory / Task API
- separate “repo truth”, “public contract”, and “still-unverified runtime assumptions”
- correct the owner design so it no longer treats task `extra_vars` support as proven

## Landed

| File | Change | Why |
|---|---|---|
| `docs/reference/investigations/IVG-SEMAPHORE-INVENTORY-API.md` | new | capture the Phase 1 contract matrix and open gaps |
| `docs/reference/investigations/INDEX.md` | add row | future agents need a discoverable investigation entry |
| `docs/reviews/feat-target-architecture/design-2026-05-26.md` | §3.1 / §3.2 / §3.4 corrected | align inventory-type assumptions and stop presenting task `extra_vars` as settled truth before runtime proof |
| `TODO.md` | target-architecture status advanced to in-progress for Phase 1 | make the branch-level next step reflect the new investigation state |

## Main Findings Carried Forward

1. `controller/semaphore/bootstrap.yml` still creates `type: "file"` inventories, so this branch is not yet using DB-backed `static` inventory.
2. `playbooks/vps/` already fixed the retained lifecycle contract to `vps_targets` + top-level `vps_task`.
3. The public contract inspected in this round is strong enough to pursue `static` inventory whole-object CRUD, but not yet strong enough to treat task `extra_vars` as a proven launch field.

## Boundaries

- did **not** run a live Semaphore mutation probe in this round
- did **not** extend `bootstrap_preflight.yml` yet
- did **not** start CF Worker implementation

## Runtime Blocker Observed

- `.venv/bin/ansible-playbook` and `.venv/bin/python3` are absent in this workspace
- Docker CLI is present, but the daemon socket currently resolves to a missing target (`/var/run/docker.sock -> ~/.docker/run/docker.sock`)

## Next Expected Work

1. Run the disposable Semaphore probe once Docker daemon access is available.
2. Add a repeatable contract gate for `GET/PUT /inventory/{id}` and task launch params.
3. Only after that, lock the Phase 2 Wizard / Worker payload contract.
