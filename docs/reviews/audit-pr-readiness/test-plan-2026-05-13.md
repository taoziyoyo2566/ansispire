# PR-Readiness 验收报告 — `feat/eda-advanced-healing` → `master`

- **日期**：2026-05-13
- **范围**：分支自 `master` 分歧后的 44 个 commit，212 文件，+10 757 / −1 406 行
- **目的**：在向 `master` 发起 PR 前，给出审查 / 代码校验 / 测试三个维度的可追溯证据
- **报告人**：Claude（本会话内实测 + 实读）

---

## 1. 三维结论速览

| 维度 | 工具 / 方法 | 结果 | 证据 |
|---|---|---|---|
| **代码校验** | `make verify`（lint + syntax + test-eda + dry-run） | ✅ exit 0 | §3 |
| **代码校验** | `yamllint`（项目源码 scoped） | ✅ 0 error | §3 |
| **代码校验** | `ansible-lint --profile production` | ✅ 0 failure / 0 warning / 95 files | §3 |
| **代码校验** | `python -m py_compile` × 6 controller files | ✅ 0 error | §3 |
| **代码校验** | bash `-n` × 全 shell 脚本 | ✅ 0 error | §3 |
| **代码校验** | JSON parse × 全 JSON 文件 | ✅ 0 error | §3 |
| **测试 L1–L3** | `make test-eda`（reactor unit + rules contract + reactor component） | ✅ 28 cases PASS in <1 s | §4 |
| **测试 L4** | `make test-eda-e2e`（disposable EDA chain） | ✅ task1 success in 22 s, total 57 s | §4 |
| **测试 L5** | `molecule test -s database`（ubuntu22 + debian12） | ✅ 全 8 阶段 Successful | §4 |
| **测试 L5** | `molecule test -s webserver`（ubuntu22 + debian12） | ✅ 全 8 阶段 Successful | §4 |
| **测试 L5** | `molecule test -s full-stack`（fullstack-ubuntu22） | ✅ 全 8 阶段 Successful，ok=23 changed=0 | §4 |
| **审查** | 按 surface 分组 diff 通读 + spot check | ✅ 无阻塞性发现 | §5 |

**整体 PR-readiness 判定**：✅ **可发**。无安全 / 正确性阻塞项；遗留事项已被 TASK-007 / TASK-008 / TASK-005 跟踪。

---

## 2. 测试金字塔现状（surface × test 矩阵）

```
                 L1 unit    L2 contract  L3 component  L4 e2e        L5 molecule
                 (pytest)   (pytest)     (pytest)      (real docker) (full role lifecycle)
─────────────────────────────────────────────────────────────────────────────────────────
common role         —          —            —             —             ✅ verify.yml
database role       —          —            —             —             ✅ verify.yml (T-C1/T-C3)
                                                                          + idempotence (T-C2)
webserver role      —          —            —             —             ✅ verify.yml
full-stack          —          —            —             —             ✅ 集成 verify.yml
infra_baseline      —          —            —             —             ❌ no scenario
ansispire_hub       —          —            —             —             ❌ no scenario
                                                                          (`make hub-deploy-check`
                                                                           间接覆盖 dry-run)
ansispire_audit     —          —            —             —             ❌ no scenario
EDA reactor      ✅ 14/14    ✅ 9/9       ✅ 5/5         ✅ disposable    —
                  test_re-   test_rules-  test_re-      controller/
                  actor.py   _contract.py actor_-       audit/e2e/
                                          component.py
EDA rules JSON      —        ✅ schema     —             ✅ rule-fire     —
                             validation                  via run.sh
db_failover         —          —            —             ⏭ disabled    —
playbook                                                  (TASK-008
                                                           placeholder)
nginx remediation   —          —            —             ❌ no L4       — (单独
                                                                          scenario 未测)
```

**Legend**: ✅ today re-verified | ⏭ intentional skip | ❌ no coverage / gap

---

## 3. 代码校验细节

### 3.1 `make verify` 链路（push gate）

```
make verify  →  lint  →  syntax  →  test-eda  →  dry-run
              │         │           │            │
              ✅ 0     ✅ stag      ✅ 28        ✅ 31s playbook
                       + prod        cases        ok=44 changed=15
                       syntax        in <1s       failed=0
                       PASS
```

**关键发现**：TODO.md 此前标注 "make verify lint blocked by vault"，**已过期**——本次实测整链路 exit 0。可能由 commit `ec93094 fix(verify): make verify chain green end-to-end` 修复。建议把 TODO.md 「已知 NOT done」中的 `make verify` 一行去掉（→ §6 Follow-up）。

### 3.2 yamllint scoped

- `yamllint .` 在仓库根有大量第三方告警（`.venv/lib/python3.12/site-packages/molecule_plugins/*`），属于 dependency code 噪音，**不属于本项目的 lint 责任面**。
- 项目源码 scoped（`roles/ inventory/ playbooks/ molecule/ controller/ extensions/ config/`）：
  - **0 error**，1 warning（`inventory/local/vault.yml` missing `---`，但该文件是 vault-encrypted 的 `$ANSIBLE_VAULT;1.1;AES256`，且**未入 git**——只有本机存在，warning 可忽略）。

### 3.3 ansible-lint

```
$ ansible-lint --profile production
Passed: 0 failure(s), 0 warning(s) in 95 files processed of 262 encountered.
Profile 'production' was required, and it passed.
```

### 3.4 Python / Bash / JSON 静态校验

- `controller/audit/{reactor,relay,test_*}.py` × 6 个 .py：`py_compile` 全过
- 全 `*.sh` 脚本：`bash -n` 全过
- 全 `*.json` 文件：`json.load()` 全过

---

## 4. 测试细节

### 4.1 EDA 软件层（L1–L3）

```
$ make test-eda
test_reactor.py            14 cases  PASS  (cooldown/match/skip/parse-error/recv)
test_rules_contract.py      9 cases  PASS  (schema validation)
test_reactor_component.py   5 cases  PASS  (semaphore_api template→id resolution + auth)
                          ─────────
                           28 cases  PASS  in <1 s
```

### 4.2 EDA 真实链路（L4）

```
$ make test-eda-e2e
==> [1/7] clean → [2/7] up → [3/7] semaphore-bootstrap → [4/7] mint EDA token
==> [5/7] recreate audit-relay + audit-reactor (pick up token)
==> [6/7] inject Disk Full event into the sink
==> [7/7] poll Semaphore tasks until status=success (≤60 s)
==> E2E PASS — task 1 status=success in 22 s (total 57 s)
```

⚠ Stack leave-running by design（端口 3320，admin/e2e_disposable_pw）。手动清理见 §6。

### 4.3 Molecule 角色级（L5）

| Scenario | Platforms | Phases | Verify 断言 |
|---|---|---|---|
| `database` | ubuntu22 + debian12 | destroy / syntax / create / prepare / converge / **idempotence** / **verify** / destroy 全 Successful | MySQL svc / port 3306 / testdb / my.cnf 存在（T-C1）/ my.cnf Ansible-managed（T-C1）/ testuser auth + query（T-C3）。T-C2 root pw dedup 由 idempotence `changed=0` 隐式覆盖。 |
| `webserver` | ubuntu22 + debian12 | 全 8 阶段 Successful | nginx config syntax / nginx :80 响应 / default index 文件 |
| `full-stack` | fullstack-ubuntu22 | 全 8 阶段 Successful，最终 PLAY RECAP `ok=23 changed=0 failed=0` | 三 role 集成：timezone / curl / appuser / nginx pkg+svc+vhost+:80 / mysql pkg+svc+:3306 / appdb 存在 / my.cnf managed / nginx∧mysql 同时 running |
| `common` | — | 本轮未跑 | （已被 full-stack 隐式覆盖 timezone/curl/UFW 核心断言） |

---

## 5. 审查发现

### 5.1 安全（🟢 全清）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 真 secret 进入 git 树？ | ❌ 无 | `git ls-files` 仅返 `.secrets.baseline`（detect-secrets 规则文件，非密文）+ `*.example.yml`。`inventory/local/vault.yml`（真 vault）未入 git。 |
| `.gitignore` 是否覆盖所有敏感面？ | ✅ 是 | 排除 `.vault_pass` / `controller/semaphore/.env` / `controller/audit/e2e/.env` / `controller/rbac/users.yml` / `inventory/**/vault.yml` / 各类 `.secrets` |
| 敏感文件权限？ | ✅ 合理 | `ansispire_hub/.env` `mode: 0600`、state dir `mode: 0700`、`/etc/mysql/mysql.conf.d/mysqld.cnf` 由模板默认 |
| 凭据是否硬编码？ | ❌ 无 | reactor 从 `SEMAPHORE_API_TOKEN` env 读；MySQL root pw 通过 `database__mysql_root_password` var；`no_log: true` 用在 root pw 任务上 |
| 防火墙变更安全性？ | ✅ 合理 | `common/security.yml` 明确 `allow loopback in on lo`（修上次 health check 误失败 bug）；UFW 仅 Debian-family 启用，default deny incoming 后才打开 lo |
| sudo / sudoers 改动？ | ✅ 受控 | 仅 `infra_baseline` 新增管理用户（`ansible_facts['os_family']` 守门，未实现的 RHEL/Alpine 分支 no-op） |

### 5.2 正确性（🟢 无阻塞，🟡 一项待跟）

| 检查项 | 结果 | 备注 |
|---|---|---|
| Ansible facts 现代访问？ | ✅ | 全部从 `ansible_os_family` 迁到 `ansible_facts['os_family']` |
| check_mode 兼容？ | ✅ | `database/install.yml` `ignore_errors: "{{ ansible_check_mode }}"`；`ansispire_hub` Provision Admin & Token 块显式 `when: not check_mode` |
| 幂等性？ | ✅ | molecule idempotence 阶段所有 scenario 全 `changed=0`；MySQL root pw 用 `check_implicit_admin: true` 处理 empty→configured pw race |
| Handler / notify 一致？ | ✅ | `Restart SSH service` 用 `listen` 模式（handler name 解耦） |
| 占位 vs 真实实现 标注？ | ✅ | `playbooks/remediation/db_failover.yml` 仅 `debug` placeholder；`extensions/eda/rules.json` 对应规则 `enabled: false` + 显式 `_disabled_reason` 引用 TASK-001 follow-up |
| 🟡 vault.yml.example warning | yamllint warning | 无入 git，可忽略；如果想消，加个 `---` 即可 |

### 5.3 设计（🟢 一致）

| 检查项 | 结果 |
|---|---|
| 变量命名空间？ | ✅ `<role>__<var>` 规范贯穿（`common__*` / `database__*` / `webserver__*` / `infra_baseline_*` / `ansispire_hub_*`） |
| 旧 alias 清理？ | ✅ `nginx_vhosts` 已彻底删除（CHANGELOG 记录 breaking-internal-only） |
| Vendor patches？ | ✅ `roles/geerlingguy.docker/` 改动符合 `docs/governance/vendor-patches.md` re-apply protocol |
| Manifest SSOT？ | ✅ `config/manifest.yml` 是 ports + image versions 唯一源；`make manifest-sync` 散播到 `.env` |

### 5.4 测试覆盖（🟡 4 项 gap，全部非阻塞）

| Gap | 影响 | 跟进 |
|---|---|---|
| `ansispire_hub` 无 molecule scenario | 中 — 该 role 是 hub 部署核心，目前只靠 `make hub-deploy-check NODE=remote` 间接 dry-run | 建议加 `molecule/hub/` scenario（systemd + docker-in-docker），non-blocking |
| `ansispire_audit` 无 molecule scenario | 低 — 行为已被 EDA L4 e2e 覆盖（reactor + relay + sink 起来即过） | 暂可不做 |
| `infra_baseline` 无 molecule scenario | 低 — 当前只 Debian 路径活跃，TASK-007 上线 RHEL/Alpine VPS 后再加更划算 | TASK-007 跟 |
| nginx remediation 无 L4 测试 | 中 — disk_cleanup 有 e2e，nginx 重启自愈无 | 建议在 TASK-001 follow-up 阶段加 |

### 5.5 CI gate（🟢 完整）

`.github/workflows/ci.yml` 三 job：
1. `yamllint` — 用 `yamllint .`（与本机 scoped 行为不同，CI 会跑全树）
2. `ansible-lint` — 运行时生成一次性 `.vault_pass` 后执行 `ansible-lint --profile production`
3. `syntax-check` — playbook syntax-check 跨 stag + prod

触发：push 到 `dev|master|hotfix/*`；PR 到 `dev|stg|master`。`stg` 故意 PR-only（branch protection 应配套，**未在本审查内验证**）。

---

## 6. Next Steps（基于本审查）

### Immediately doable（不阻塞 PR，可作为 PR description 的勾选项）
- [Claude] TODO.md「已知 NOT done」中删掉 `make verify` 一行（已绿）—— 1 min
- [User] 清理 e2e leave-running stack：
  ```
  docker compose -f controller/audit/e2e/compose.e2e.yml \
    -p ansispire-e2e \
    --env-file controller/audit/e2e/.env \
    --env-file controller/audit/e2e/.secrets down -v
  ```

### Blocked（需用户授权）
- `git push -u origin feat/eda-advanced-healing`
- `gh pr create --base master --title ... --body ...`（PR body 可基于 §1 速览 + §2 矩阵 + §6 勾选项起草）

### Deferrable
- `molecule test -s common` 单独跑（已被 full-stack 覆盖，完备性补充）
- `molecule/hub/` scenario 新增（覆盖 ansispire_hub）—— 建议在 TASK-007 启动时同步做
- nginx remediation L4 测试 —— TASK-001 follow-up
- 验证 GitHub branch protection 是否真把 `stg` 限为 PR-only
