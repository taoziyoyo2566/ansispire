# feat-git-history-hygiene — Engineering Plan: Strip AI Co-Author Trailers Before GitHub Publish

**Date**: 2026-04-16  
**Level**: Engineering  
**Type**: Implementation (history rewrite + verification)  
**Status**: Landed (see phase1 changelog)  
**Document path**: `docs/reviews/feat-git-history-hygiene/plan-2026-04-16.md`

---

## 1. System Intent Clarification

- **Kind of system**: engineering hygiene — make git history suitable for GitHub publication.
- **Boundaries**:
  - Only git metadata (commit messages) is rewritten.
  - No code/content edits required to achieve the objective (besides these docs).
  - No network egress required; repo had no configured remote at time of change.
- **NFR priority**: correctness (remove unwanted trailers everywhere) > safety (backup + verifiable outcome) > speed.

---

## 2. Current-State Assessment (at time of starting)

- Branch: `master`
- Total commits: 22
- Commits containing `Co-Authored-By:` trailers: 13
- Target to remove: any `Co-Authored-By: ... <noreply@anthropic.com>` line
- Tooling: `git filter-repo` not installed → use `git filter-branch` fallback

---

## 3. Goal / Judgment Criteria

- `git log --all --format=%B` contains **zero** occurrences of:
  - `Co-Authored-By:`
  - `noreply@anthropic.com`
- Commit messages are normalized for readability:
  - trim trailing whitespace
  - collapse repeated blank lines
  - remove trailing blank lines
- A recoverable checkpoint exists (pre-rewrite backup branch).

---

## 4. Plan (single phase)

1. Freeze working tree (avoid mixing concerns).
2. Create safety backup branch: `backup/pre-strip-coauthor-2026-04-16`.
3. Rewrite history:
   - Remove matching `Co-Authored-By:` lines referencing `noreply@anthropic.com`
   - Normalize whitespace/blank lines in commit messages
4. Verify by grep on `git log --all --format=%B`.
5. Cleanup `filter-branch` remnants (`refs/original/*`) and prune unreachable objects.
6. Write phase changelog.

