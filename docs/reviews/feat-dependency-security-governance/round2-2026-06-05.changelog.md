# feat-dependency-security-governance — Round 2: WU-1 version-truth model

**Date**: 2026-06-05
**Branch**: `feat/dependency-security-governance` (**dev-based**, security-priority fast-track per W-R22)
**Level**: 🔴 [L2] Architecture / Governance — WU-1 slice (version truth only).

**Reference**: [`plan-2026-06-03.md`](./plan-2026-06-03.md) §4 WU-1 · [`round1-2026-06-03.changelog.md`](./round1-2026-06-03.changelog.md) (plan creation).

## §0 Pre-execution Checklist (W-R18 framework best-practice pre-check)

- **Sources consulted**: `pyproject.toml` (`[tool.uv]`, optional-deps), `requirements.txt`, `requirements.yml`, `config/manifest.yml`; tool availability probe (`uv`/`pip-audit`/`trivy`/`grype`/`osv-scanner`).
- **Conclusion — direction matches, with one carrier decision made for us**: the project already declares `[tool.uv]` ⇒ uv is the adopted package manager. The plan listed `uv.lock` / `pip-compile constraints` / `release manifest` as *candidate* freeze carriers; the pre-check resolves it: **use `uv.lock`** (uv-native). Adding a parallel `pip-compile`/constraints layer on an already-uv project is the W-R18(b) "layer on the existing tool" anti-pattern → rejected.
- **Collections** are already exact-pinned in `requirements.yml` ⇒ no new carrier needed there.

## Landed

> **Rebuild note (2026-06-06)**: this branch was re-created **from `dev`** (not stacked on `feat/target-architecture`) as a W-R22 security fast-track that **carries only NEW files — zero modifications to existing tracked files** (plan owner-branch note). The earlier round-2 cut also touched `TODO.md` and `CHANGELOG.md`; those edits are **intentionally dropped here** to keep the branch independently mergeable and conflict-free with other active feat branches. The TASK-009 ledger + `[Unreleased]` entry sync forward when this merges to `dev` (trunk-sync), not on this branch.

| File | Change | Intent |
|---|---|---|
| `docs/governance/dependency-security.md` | new | WU-1 deliverable: two-mode model (dev-floor vs release-freeze), per-surface SSOT inventory, freeze-carrier decision per surface, "which file for which surface" map |
| `docs/reviews/feat-dependency-security-governance/plan-2026-06-03.md` | new (in this branch); status DRAFT→ACTIVE; owner-branch note | reflect WU-1 landing + dev-based fast-track reality |
| `docs/reviews/feat-dependency-security-governance/round1-2026-06-03.changelog.md` | new (in this branch) | planning-bootstrap record |

## Decisions recorded in the doc

1. **Release freeze applies to CI / release / audit only**, not all local dev (plan §4 open decision resolved; rationale = don't make contributor setup brittle, plan §7 / W-R13(b)).
2. **Python freeze carrier = `uv.lock`** (W-R18).
3. **Container release evidence = tag pin + immutable digest** recorded at release cut; `manifest.yml` stays tag-based for daily use.

## NOT done (explicit)

- **`uv.lock` not generated**: needs `uv` + dependency-resolution network access; absent in this workspace. Documented as the WU-1 runtime follow-up (doc §3.1 / §6 leaves that box unchecked). The Python release set is *declared*, not yet *materialized*.
- **WU-2..5 untouched**: scanners, compatibility matrix, Semaphore upgrade policy, waivers — separate WUs (plan §6 order: WU-1 → WU-4 → WU-2 → WU-3 → WU-5).
- **No CI wiring**: no scan gate added this round (that's WU-2).

## Self-check (W-R15 3-gate)

- **Gate 1 — direction**: deliverable matches plan §4 WU-1 (version truth + freeze model + SSOT map); did not stray into WU-2 scanner territory. ✅
- **Gate 2 — syntax/static**: doc is Markdown (no executable syntax); cross-checked every version literal against its real SSOT file (`requirements.yml` pins, `manifest.yml` `semaphore_pinned: v2.18.2` / `audit_python_pinned: 3.12-alpine`, `pyproject.toml` `[tool.uv]`). ✅
- **Gate 3 — functional**: the only executable claim is the `uv lock` / `uv sync --frozen` procedure — **NOT run** (no `uv` in workspace). Recorded as env-blocked, not passed. ⚠️

## Follow-Up / Next Steps

- **Runtime (provisioned env)**: `uv lock` → commit `uv.lock`; closes the last WU-1 box.
- **Next WU (in-repo, no daemon)**: WU-4 Semaphore upstream security policy doc (`docs/governance/semaphore-upgrade-policy.md`) — next per plan §6, and like WU-1 it is documentation-first so it can proceed in this workspace.
- **Merge order**: this branch is **dev-based and independent** (W-R22 security fast-track) — it merges to `dev` on its own, ahead of and unblocked by other feat branches. After it lands, other active branches pick up the new governance doc via normal trunk-sync (`git merge origin/dev`).
