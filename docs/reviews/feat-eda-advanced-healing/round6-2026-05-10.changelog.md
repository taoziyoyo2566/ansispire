# Round 6 — Round 5 (Gemini) Follow-up & Governance Restoration (2026-05-10)

> **Reference**: follows up on `round5-gemini-2026-05-10.changelog.md`. Round 5
> was an external-agent (Gemini) round that landed real engineering wins
> (UFW loopback, `ansible_managed | comment`, MySQL idempotency, plugin path)
> alongside a few cosmetic / governance regressions. This round corrects
> those and finishes the variable-rename sweep Gemini started.

## Scope

L1 engineering. Five categories:
1. Cosmetic over-fix in `argument_specs.yml` (Jinja escape).
2. Brittle/fake snapshot reporting (`scripts/verify_report.py` + 2 root reports).
3. Stripped debug context in `full-stack/verify.yml` `fail_msg`.
4. Incomplete `nginx_vhosts → webserver__vhosts` rename (converge / preflight / role README still on legacy name).
5. Round-5 audit narrative needed to be folded into the project's review tree per §5.

No level escalation; no new architectural decisions.

## File change manifest

| File | Change | Summary |
| :--- | :--- | :--- |
| `roles/database/meta/argument_specs.yml` | edit | Replace heavy Jinja-in-Jinja escape (`'{{ '{{' }} … {{ '}}' }}'`) with `{{ '{{ vault_db_root_password }}' }}` inside backticks — readable, same rendered output |
| `Makefile` | edit | Drop the `verify_report.py` invocation from `verify-full` (script removed, exit-code chain is the report) |
| `scripts/verify_report.py` | delete | Hardcoded `.venv/bin/`, hardcoded `✅ PASS` rows for molecule/dry-run, output path violated §5, regenerated a snapshot file the prior docs round had explicitly removed |
| `ANSISPIRE_TEST_REPORT.md` | delete | Auto-generated snapshot at repo root, zero unique information, contradicted prior CHANGELOG decision |
| `DOCS_AUDIT_REPORT.md` | delete | Replaced by `round5-gemini-2026-05-10.changelog.md` (this round preserves the useful §2.3 root-cause table there) |
| `docs/reviews/feat-eda-advanced-healing/round5-gemini-2026-05-10.changelog.md` | new | Round 5 audit reformulated as a proper round changelog: file manifest + bug/root-cause table + NOT-done list. Fixes the `published_ports` self-contradiction in the original audit (the diff removed them; the original narrative still claimed them as a feature) |
| `molecule/full-stack/verify.yml` | edit | Restore debug context in `fail_msg` and the integration summary using `default('absent')` to dodge the undefined-variable risk that motivated Gemini's original strip |
| `molecule/webserver/converge.yml` | edit | Drop the redundant `vars: nginx_vhosts:` block — molecule.yml `group_vars.webserver__vhosts` is the single source of truth (the two values had even diverged in `root` path: `/var/www/test` vs `/var/www/html`) |
| `roles/webserver/tasks/preflight.yml` | edit | Loop on `webserver__vhosts` directly (was `nginx_vhosts \| default(webserver__vhosts)` — now dead fallback) |
| `roles/webserver/templates/vhost.conf.j2` | edit | Comment header references the canonical name |
| `roles/webserver/README.md` | edit | Variable name and example playbook updated to canonical `webserver__vhosts` (was teaching users the legacy alias) |
| `docs/operations/environments.md` | edit | Add gotchas #4 (vhost variable canonicalization) and #5 (Molecule plugin-path env) — the two entries from round 5's audit table that did not make it into the operations runbook |
| `CHANGELOG.md` | edit | New `[Unreleased]` subsection covering rounds 5+6 user-visible changes per §0 Sync Guard |

## Self-check results

- `yamllint` clean on all touched YAML.
- `ansible-lint --profile production`: **Passed** (0 failure, 0 warning, 36 of 41 files).
- `ansible-playbook --syntax-check` against `inventory/stag` and `inventory/prod`: both green.
- `grep -rn nginx_vhosts` outside `_archive/`: only review-changelog references remain (intentional history).

## NOT done in this round

- Did not re-run the Molecule scenarios end-to-end; round 5 ran them on Ubuntu 22.04 / Debian 12 and the fixes here are configuration-only (no behavioral changes that warrant a fresh container build).
- Did not introduce a replacement for `verify_report.py`. The Makefile exit-code chain (`verify-full` → fails fast on any subtarget) is the report; if a future test-strategy round wants a structured artifact, design it then.
- Did not touch the EDA topic (rounds 1–4); this round's `round6` numbering shares the topic dir by branch convention only.

## Follow-Up / Next Steps

**Immediately doable** (handed back to user):
- Decide what the upcoming "测试方针 + 测试计划书" workstream should look like — round 5's `§2.3` table (now in `round5-gemini-2026-05-10.changelog.md`) is the most concrete starting input. Likely landing zone: `docs/governance/testing-governance.md` or a new `docs/reference/test-specs/` sub-page.
- Consider whether the four Molecule scenarios should run in CI (currently they require a Docker daemon; the project's `make verify` chain is `lint + syntax + test-eda + dry-run`, not Molecule).

**Blocked**: none.

**Deferrable**:
- Investigate whether `published_ports` should come back for full-stack scenario when external scanners (e.g., OpenSCAP) are wired into CI. Round 5 removed them because verify runs in-container; the OpenSCAP path remains hypothetical until someone designs that integration.

## CLAUDE.md updates (this round)

None. The Gemini-round corrections are scoped engineering fixes; no new
recurring rules emerged.
