# TASK-007 Round 1 — Multi-OS Target Fleet (RHEL family) Changelog

Reference: [`plan-2026-05-19.md`](./plan-2026-05-19.md). Branch: `feat/multi-os-target-fleet` (base: `dev`).

## Phases delivered

| Phase | Commit | Scope |
|---|---|---|
| 0 | `dff2a3c` | plan-doc landed; branch created from `origin/dev` |
| 1 | `1ae2a35` | WU-A — 4 VPS pinned in `inventory/hosts.ini`; ping reaches all |
| 2 | `b0dc08a` | WU-B — `redhat.yml` + `vars/RedHat.yml` + `main.yml` refactor + `deploy_target.yml` + inventory `ansible_user` flip to steady-state |
| 2.5 | `f63a68f` | Codex round 1 review fixes (3 of 4 — `python3-libsemanage`, Permissive boolean, fresh+check stat probe) |
| 2.5 | `c73bf48` | Plan-doc sync: Codex Finding 4 (`ANSIBLE_USER` knob) → Phase 3 WU-D scope |
| 2.6 | `bc21e30` | Codex round 2 review fixes (`meta: end_host` for fresh+check; `check_mode: false` on `getenforce`) |
| 3 | `747f5b4` | WU-D — `make target-deploy` + `target-ping` Makefile wrappers; WU-E — `playbooks/ping_targets.yml` + Semaphore "Ping all targets" template registration; prod inventory populated |
| 4 | _(this commit)_ | WU-F — Sync Guard updates + CHANGELOG + TODO + round1 changelog |

## File change manifest (cumulative)

### NEW
| File | Lines | Purpose |
|---|---|---|
| `roles/infra_baseline/tasks/redhat.yml` | ~95 | RHEL-family dnf path: python3.11 install + pivot, Docker CE repo, SELinux container_manage_cgroup, fresh+check safety, systemd start |
| `roles/infra_baseline/vars/RedHat.yml` | ~18 | Per-family overrides: dnf repo URL, python3.11 pkg/path, `infra_baseline_mgr_admin_group=wheel` |
| `playbooks/deploy_target.yml` | ~38 | Wrapper applying `infra_baseline` to `hosts: targets` only |
| `playbooks/ping_targets.yml` | ~33 | Read-only connectivity probe across the fleet (ping + docker version) |
| `docs/reference/feature-map/multi-os-fleet.md` | ~75 | Feature-map entry for the data-plane fleet |
| `docs/reviews/feat-multi-os-target-fleet/plan-2026-05-19.md` | ~345 | Plan-doc (the SSOT for design decisions D1–D9) |
| `docs/reviews/feat-multi-os-target-fleet/round1-2026-05-19.changelog.md` | (this file) | Round 1 evidence chain |

### MODIFIED
| File | Change | Summary |
|---|---|---|
| `roles/infra_baseline/tasks/main.yml` | Task-order refactor | Python assert moved post-per-family; RHEL fail-stub → `include_tasks: redhat.yml`; Docker service start moved into Debian block; user creation parameterized with `infra_baseline_mgr_admin_group` |
| `roles/infra_baseline/defaults/main.yml` | +5 lines | Default `infra_baseline_mgr_admin_group: sudo` (Debian); overridden in `vars/RedHat.yml` |
| `inventory/hosts.ini` | +20 -7 | `[targets_*]` populated with 4 real VPS; `[targets:vars]` common config (ansible_user, key, python, host-key acceptance); steady-state `ansible_user=ansible` |
| `inventory/prod/hosts.ini` | +24 -5 | Same `[targets_*]` populated (prod is self-contained, no auto-merge); `[targets:children]` parent declaration added for `[targets:vars]` binding |
| `Makefile` | +43 lines | `target-deploy` + `target-deploy-check` + `target-ping`; `TARGET_NODE` accepts group or alias; `ANSIBLE_USER=root` env override for fresh-host bootstrap |
| `controller/semaphore/bootstrap.yml` | +41 lines | "Ping all targets" Job Template registration (idempotent); bootstrap-complete summary extended with TASK-007 section |
| `ARCHITECTURE.md` | +1 line in §2 / +1 in §3 | Inventory taxonomy note updated; feature-map link added to §3 |
| `README.md` | +1 row in Doc Map | `make target-deploy` operator entry pointing at multi-os-fleet feature-map |
| `CHANGELOG.md` | New `[Unreleased]` subsection | "Multi-OS target fleet" entry |
| `TODO.md` | TASK-007 → ✅ Completed; new TASK-007.B (Alpine) + TASK-007.C ([hub_remote] cleanup) follow-ups | Roadmap progression |
| `docs/reference/feature-map/INDEX.md` | 5 surgical edits | `infra_baseline` row → ✅ Debian + RHEL; `deploy_target.yml` + `ping_targets.yml` added to playbook matrix; Target Taxonomy section reflects real hosts; Make UX adds `target-deploy`; §9 closes RHEL bullet |

## Decisions executed (per plan §3)

| ID | Decision | Outcome |
|---|---|---|
| D1 | Per-family `include_tasks` pattern | ✅ Applied for RHEL (`redhat.yml`); Debian deferred (still inline `block:`) |
| D2 | `dnf config-manager` over hand-rolled `dnf_repository` | ✅ Used vendor `.repo` URL with `creates:` for idempotency |
| D3 | (Alpine: stable community repo over edge) | ⏸ N/A this round — Alpine deferred |
| D4 | Docker service start moved into per-family blocks | ✅ Debian block + RHEL `redhat.yml` each manage their own |
| D5 | `vars/<OS_family>.yml` for family-specific overrides | ✅ `vars/RedHat.yml` created |
| D6 | New `deploy_target.yml` (not reuse `site.yml`) | ✅ Created; targets `hosts: targets` only |
| D7 | Semaphore "Ping all targets" template in scope | ✅ Registered in `bootstrap.yml` |
| D8 | Python 3.11 install + pivot for RHEL | ✅ `redhat.yml` first task + set_fact + setup re-gather; main.yml assert moved to end |
| D9 | `[hub_remote]` cleanup is OUT of scope (ans-hk01 dead) | ✅ No change to `[hub_remote]`; tracked as TASK-007.C |

## NOT done (explicit)

- **Alpine OS-family branch** — `main.yml:70-78` fail-stub unchanged. Tracked as **TASK-007.B**.
- **`[hub_remote]` cleanup** — `ans-hk01` IP still references the now-wiped machine. Tracked as **TASK-007.C**.
- **Per-OS Molecule scenarios for RHEL** — current 4 Molecule scenarios cover Debian only.
- **End-to-end Semaphore "Ping all targets" execution** — declared in `bootstrap.yml` but not materialized (needs running Semaphore + hub-side SSH key on targets).
- **Codex Finding 4 deferred work** — `TARGET_NODE` does accept single aliases (delivered Phase 3); the `ANSIBLE_USER=root` knob was implemented but not tested end-to-end against an actually-fresh RHEL host (current 4 VPS are all already baselined).

## Self-check (W-R15 3 gates across all commits)

All gates run clean in iteration 1 at every commit boundary:

| Commit | Gate 1 (direction) | Gate 2 (static) | Gate 3 (functional) |
|---|---|---|---|
| `dff2a3c` | ✅ plan matches user approval | ✅ yamllint, markdown OK | n/a (doc-only) |
| `1ae2a35` | ✅ matches plan §8.1 | ✅ `ansible-inventory --list` parses | ✅ `ansible targets -m ping` → 4× pong |
| `b0dc08a` | ✅ matches plan D1+D4+D5+D8 | ✅ yamllint + ansible-lint --profile production | ✅ all 4 hosts: first-run clean (d13/u24 ok=13 changed=10, rocky9/alma9 ok=20 changed=9, failed=0); idempotent re-run ok same / changed=0; post-flip `ansible_user=ansible` re-run also 0-changed |
| `f63a68f` | ✅ each of 3 findings addressed | ✅ ansible-lint passed | ✅ dry-run on baselined rocky9 no longer fails; real-run idempotent |
| `c73bf48` | ✅ plan sync only | ✅ markdown OK | n/a (doc-only) |
| `bc21e30` | ✅ end_host + check_mode: false correctness | ✅ ansible-lint passed | ✅ dry-run on baselined rocky9 now previews SELinux block end-to-end (no SKIPPED); real-run idempotent (ok=21 changed=0) on both rocky9 + alma9 |
| `747f5b4` | ✅ Codex F3+F4 addressed | ✅ yamllint + ansible-lint OK; prod inventory --graph correct | ✅ `make target-ping` 4× pong; `make target-deploy TARGET_NODE=rhel` idempotent (rocky9+alma9 ok=21 changed=0); `make -n target-deploy-check TARGET_NODE=rocky9 ANSIBLE_USER=root` resolves to expected ansible-playbook command |
| _(Phase 4)_ | ✅ Sync Guard targets all touched | _(to verify in this commit)_ | _(make verify will run)_ |

W-R17 iteration cap: not exhausted at any boundary.

## CLAUDE.md updates (this round)

None. TASK-007 is engineering work that fits within existing W-R1/W-R7/W-R10/W-R13/W-R15/W-R17/W-R18 rules. No new project-wide pattern emerged that warrants a CLAUDE.md amendment.

## Follow-Up / Next Steps

**Immediately doable**
- Open PR `feat/multi-os-target-fleet` → `dev` (mergeable; reviews welcome).
- Watch CI on the new branch (12 jobs from `.github/workflows/ci.yml`).

**Blocked (user decision)**
- TASK-007.B (Alpine): need user to provision ≥1 Alpine VPS.
- TASK-007.C (`[hub_remote]` cleanup): need user to decide whether to bring up a new remote hub or accept hub-local-only state.
- PR #14 (`fix/codex-cross-compare-hygiene`) — GitGuardian still fails on `compose.yml:16` false-positive; independent of this task.

**Deferred (P2/P3)**
- Per-OS Molecule for `infra_baseline.redhat`.
- End-to-end Semaphore "Ping all targets" execution test (requires running hub).
- `TARGET_NODE=<alias> ANSIBLE_USER=root` empirical test against a freshly-reimaged RHEL host.
