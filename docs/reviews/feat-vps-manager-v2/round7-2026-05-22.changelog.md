---
# Round 7 Changelog — feat/vps-manager-v2 (add-host triple-entry + post-onboard SSH alias write)

Reference: [`plan-2026-05-22.md`](./plan-2026-05-22.md) (supersedes [`plan-2026-05-17c.md`](./plan-2026-05-17c.md))
Branch: `feat/vps-manager-v2` (rounds 2–6 closed at `6011275`; R7 wip at `0eb7ec8`; R7 final at `5efc6e1`)
Date: 2026-05-22
Level: **[L1] Engineering**

---

## 1. File Change Manifest

| File | Change | Summary |
|---|---|---|
| `plugins/vps_runner/vps_runner.py` | M | +184 lines — 4 new helpers (`prompt_new_host`, `write_ssh_config`, `delete_ssh_config`, `check_include_directive`); helpers use `None` sentinels for default dirs so tests can monkeypatch module-level constants. |
| `plugins/vps_runner/cli.py` | M | `add-host` argparse `alias` → optional; new `_cmd_add_host` wizard branch (TTY check, prompt collection, optional onboard chain); new private `_write_local_ssh_alias` helper invoked from `_cmd_onboard` on `summary.status == "successful"`; `_cmd_remove` always calls `delete_ssh_config` after dropping host_vars. |
| `plugins/vps_runner/tests/test_vps_runner.py` | M | +275 lines — 15 new tests (T1–T15): `write_ssh_config` render/idempotent/dir-mode; `delete_ssh_config` exists/missing; `check_include_directive` present/absent/commented/missing; `prompt_new_host` happy/validation-retry/KeyboardInterrupt; CLI integration for wizard mode / flag-mode regression / onboard success path / onboard failure path / remove cleanup. |
| `Makefile` | M | `vps-add-host` target — drops hard `ALIAS+IP` requirement; partial-flag (only ALIAS or only IP) rejected; no-arg invocation passes through to CLI wizard. |
| `plugins/vps_runner/examples/host_vars.yml.template` | A (new) | Slim 4-user-fillable-fields template for manual third-mode entry. |
| `plugins/vps_runner/playbooks/templates/ssh_config_entry.j2` | M | DO-NOT-EDIT line added to header (matches plan §5 R1 commitment that was previously aspirational). |
| `docs/operations/vps-runner.md` | M | §3.1b rewritten — three input modes with worked examples; SSH-key-separation table; §4 step 7 updated to per-alias `<alias>.conf` (was the unimplemented `ansispire.conf` single-file promise). |
| `docs/reference/feature-map/vps-runner.md` | M | `add-host` Actions row updated (three modes); new "Post-onboard side effect" subsection under §Architecture describes operator-key local SSH write + Include warn-only + remove cleanup. |
| `CHANGELOG.md` | M | New `[Unreleased]` section "VPS Runner: add-host triple-entry + post-onboard SSH alias write (feat/vps-manager-v2 Round 7, 2026-05-22)". |

---

## 2. Intent per change

| File group | Intent |
|---|---|
| `vps_runner.py` helpers | Add the four primitives the CLI needs (wizard prompt collection + per-alias SSH config write/delete + Include directive sniff). Two-key separation enforced at the constant level: `DEFAULT_PERSONAL_IDENTITY_FILE = ~/.ssh/id_ed25519`, never touches `vps_runner_defaults.identity_file`. |
| `cli.py` changes | Plumb the helpers into existing `add-host` / `onboard` / `remove` CLI dispatch without breaking R6 backward-compat (T12 explicitly guards this). |
| Tests | Cover every new behaviour at unit level (T1–T10) and every cross-cutting CLI integration path (T11–T15). T8–T10 use a `_FakeStdin` class + `monkeypatch.setattr("builtins.input", …)` rather than fragile real-stdin manipulation. |
| Makefile | Make the three modes accessible via `make vps-add-host` (the canonical Make wrapper). |
| `examples/host_vars.yml.template` | Provide the discoverable starting point for hand-write third mode. Slim — only the 4 user-fillable fields; status/os/last_run/etc. left for the CLI/onboard to populate. |
| `ssh_config_entry.j2` | Make the "managed; do not edit" warning explicit (mitigates risk R1 in plan §5). |
| Docs | Replace the documented-but-unimplemented `~/.ssh/config.d/ansispire.conf` promise with the actual per-alias behaviour; surface the three input modes; record the key separation contract. |
| `CHANGELOG.md` | Sync Guard #4 — user-visible CLI behaviour change (wizard mode, post-onboard side effect) requires an `[Unreleased]` entry. |

---

## 3. NOT done (deliberate)

Per plan §4:

- ❌ `Include config.d/*` auto-injection into `~/.ssh/config` — warn-only; user's main config is operator turf.
- ❌ Bulk YAML manifest import for many hosts at once.
- ❌ Wizard mode for `modify` / `audit` (flag-only by design; not requested).
- ❌ Legacy `~/.ssh/config.d/ansispire.conf` migration to per-alias files.
- ❌ `stag` / `prod` inventory tree population.
- ❌ Round-trip preservation of `host_vars/<alias>.yml` and `hosts.yml` comments — same R6 limitation (a `ruamel.yaml` upgrade is a separate future TASK). Surfaced in incidental form during Canary 2 (hosts.yml lost the leading comment block when wizard rewrote it via PyYAML); the file was restored via `git checkout` before commit and the residue is fully consistent with plan §5 R8.
- ❌ `add-host --from-template` flag — template path is intentionally "cp + edit + add to hosts.yml"; no CLI involvement needed.
- ❌ TASK-007 `inventory/hosts.ini[targets_*]` ↔ `inventory/vps_runner/<env>/` reconciliation — parallel coexistence accepted by operator 2026-05-22.
- ❌ External (codex / ultrareview) review for R7 — W-R20 not triggered (no destructive sinks introduced). The whole-branch merge-to-`dev` event is the W-R20 gate for the v2 branch's lifecycle.

---

## 4. Self-Check (W-R15 3-Gate, single iteration, clean)

### Gate 1 — Direction

7 in-scope files plus the new `plugins/vps_runner/examples/` directory. Diff stat:

```
 Makefile                                           |  13 +-
 docs/operations/vps-runner.md                      |  67 +++--
 docs/reference/feature-map/vps-runner.md           |  10 +-
 plugins/vps_runner/cli.py                          | 100 +++++++-
 .../playbooks/templates/ssh_config_entry.j2        |   2 +-
 plugins/vps_runner/tests/test_vps_runner.py        | 275 +++++++++++++++++++++
 plugins/vps_runner/vps_runner.py                   | 184 ++++++++++++++
 CHANGELOG.md                                       |  10 +
 plugins/vps_runner/examples/host_vars.yml.template (new)
```

Every changed path matches plan §1.2. No collateral edits to `inventory/`, `playbooks/*.yml`, or any R2–R6 code. ✓

### Gate 2 — Syntax / static

```
.venv/bin/python -m py_compile plugins/vps_runner/{vps_runner,cli}.py plugins/vps_runner/tests/test_vps_runner.py    → PY_COMPILE_OK
make vps-runner-syntax            → all 4 playbooks pass syntax-check
.venv/bin/ansible-lint plugins/vps_runner/playbooks/  → Passed: 0 failure(s), 0 warning(s) in 9 files. Profile 'production' required, passed.
make yamllint                     → 1 pre-existing warning on plugins/vps_runner/plugin.yaml (missing `---`); not introduced by R7
```

✓

### Gate 3 — Functional

```
make test-vps-runner              → 48 passed, 3 deselected in 0.31s
make test-vps-runner-integration  → 3 passed, 48 deselected in 34.26s
```

Manual canaries (plan §1.4):

| # | Canary | Outcome |
|---|---|---|
| 1 | `echo "" \| python -m plugins.vps_runner.cli add-host --env dev` | rc=2, flag-mode hint printed; no inventory mutation |
| 2 | Wizard via PTY: `test-r7-canary / 192.0.2.50 / ↩ / ↩ / n` | `host_vars/test-r7-canary.yml` written with 4 fields; alias added to `hosts.yml`; onboard prompt offered + declined; rc=0. (Cleanup: `python -m plugins.vps_runner.cli remove test-r7-canary --env dev --yes` returned rc=0.) |
| 3 | Mocked `run_playbook` returning `successful` for `onboard test-r7-onboard` (with tmp `DEFAULT_SSH_CONFIG_DIR` / `DEFAULT_SSH_MAIN_CONFIG`) | `~/.ssh/config.d/test-r7-onboard.conf` (tmp) written, chmod 0600, content shows `IdentityFile /home/netcup/.ssh/id_ed25519`, DO-NOT-EDIT header present, `ansispire_ed25519` absent |
| 4 | Covered by T15: `remove` deletes `~/.ssh/config.d/<alias>.conf` regardless of `--cleanup-remote` | T15 pass |

All three gates ran clean in the SAME iteration (no fix-and-retry loop required). ✓

---

## 5. CLAUDE.md updates (this round)

None. R7 introduced no new methodology rules. The Round 7 round-cycle is itself a clean instance of W-R1 (plan-first), W-R7 (auto-continuation through the impl phases after explicit user GO), W-R15 (3-Gate single-iteration clean), W-R16 (visible progress on the 34 s integration test was minimal but expected; longer ones streamed phase markers), W-R18 (best-practice pre-check executed in plan §0.6 before any plan body was written), and W-R21 (dev→v2 sync merge completed prior to implementation — preceding commit `420495b`).

---

## 6. Follow-Up / Next Steps

### Immediately doable (no blockers)

- **(user)** Optionally push `feat/vps-manager-v2` to origin: 3 commits ahead (`4be54b4` plan + `420495b` merge + `5efc6e1` R7 impl). Push is reversible by force-with-lease-OID (W-R19) but ordinarily this branch's pushes are pure fast-forward additions. Decision is yours; not required for the round to be "done".
- **(claude, on next session)** Pre-`merge-to-dev`: trigger external review per W-R20 (the whole-branch event is the W-R20 gate for v2). Expected gates: `/ultrareview <PR#>` after PR opened.
- **(user)** Live PTY test of the wizard end-to-end against one dev VPS once SSH access to dev fleet is restored. Confirms onboard-then-write-SSH-alias loop works on real hosts (T13 covers the mocked path; real run validates the playbook + key write together).

### Blocked

- **3-host concurrency real test** (carried over from feature-map "Open / Pending") — still blocked on dev VPS SSH `Permission denied (publickey)` per [`docs/reference/feature-map/vps-runner.md` §Open](../../reference/feature-map/vps-runner.md). Not an R7 blocker but worth keeping in view.

### Deferrable / removable

- `requirements.txt` Jinja2 transitive-dependency check — R7 confirmed Jinja2 is present via `PyYAML` / `ansible-runner` transitive deps, but an explicit `Jinja2>=…` line would make the dependency unambiguous. Not urgent; deferred until requirements.txt is touched for other reasons.
- `host_vars/<alias>.yml` round-trip comment preservation — separate TASK if/when `ruamel.yaml` migration is undertaken; not Round 7 scope.

---

*Round 7 closed cleanly on commit `5efc6e1`. The v2 branch is ready for follow-on work (3-host live verification) or for the merge-to-dev workflow once feature dev + testing across all surfaces is complete per [[feedback_branching_model]].*
