# Changelog — feat/vps-manager-v2 Round 4-b (Cutover)

> Reference: [`plan-2026-05-16.md`](./plan-2026-05-16.md) §4 T2.10 (destructive cutover)
> Branch: `feat/vps-manager-v2` (from dev, local-only — no push)
> Round: 4-b of 4 (FINAL)
> Date: 2026-05-16
> Prior rounds: [`round2`](./round2-2026-05-16.changelog.md) · [`round3`](./round3-2026-05-16.changelog.md) · [`round4-a`](./round4-a-2026-05-16.changelog.md)

---

## 0. User authorization

User explicitly accepted "test-only parity" trade-off (live SSH verification blocked by `Permission denied (publickey)` on `deploy@82.152.164.147`). 22 tests (incl. real ansible-runner integration vs TEST-NET-1) + ansible-lint production profile + manual code review treated as sufficient parity evidence to proceed with destructive cutover. Quote: "现在就跳 Round 4-b cutover".

## 1. File change manifest

| File | Type | Summary |
|---|---|---|
| `plugins/vps_manager/` (33 files) | **D** | `git rm -r` entire legacy plugin directory (README, `__init__.py`, `cli.py`, `vps_manager.py`, 6 examples, 1 legacy/, 6 playbooks, 2 templates, syntax_inventory.yml, 2 tests). |
| `docs/operations/vps-manager.md` | **D** | Legacy operator guide removed. |
| `docs/reference/feature-map/vps-manager.md` | **D** | Legacy module spec removed. |
| `docs/reference/test-specs/vps-manager-unit.md` | **D** | Retired TSVS-VPS-MANAGER-UNIT-001 file removed (test target no longer exists). |
| `Makefile` | M | Dropped 9 legacy targets (`test-vps-manager`, `vps-manager-syntax`, `vps-new`, `vps-recover`, `vps-submit`, `vps-tasks`, `vps-manager-init`, `vps-manager-process`, `vps-manager-validate`) + corresponding `.PHONY` entries + `verify` chain entries + `VPS_EXTRA_GOAL` / `VPS_RECOVER_ALIAS` / `VPS_SUBMIT_SELECTOR` helper block (~12 lines). |
| `docs/reference/feature-map/INDEX.md` | M | §2.1 — removed 6 vps_manager playbook rows; §3.4 — removed `vps_manager` block (kept `vps_runner`); §7 — removed VPS Manager testing line; §8 — removed VPS Manager Make UX row; §9 — removed cutover-pending line. Footer date updated to "Round 4-b: vps_manager cutover". |
| `ARCHITECTURE.md` | M | §2 Plugin Plane — dropped legacy sentence (kept vps_runner-only); §3 module list — dropped legacy bullet. |
| `README.md` | M | §Capabilities — dropped "Replaces the legacy..." trailer; §Document Map — collapsed dual-track rows to single vps-runner row; §operational commands — dropped vps-manager.md link. |
| `CHANGELOG.md` `[Unreleased]` | M | Added "VPS Manager → VPS Runner cutover (2026-05-16, round 4-b) ⚠ BREAKING" section ahead of Round 2–4-a entry, with `Removed` / `Migration` / `Why no live 3-host verification` breakdown; marked legacy MVP entry "REMOVED in Round 4-b above". |
| `docs/governance/test-plan.md` | M | §2 table row vps_manager → vps_runner; §3.x quality-attribute Python row updated count; §4.12 rewritten for vps_runner (12 unit + 9 CLI + 1 integration); §5 G10 row reworded for vps_runner real-node concurrency gap. |
| `docs/governance/operational-truths.md` | M | High-Latency Optimization carrier line points at `plugins/vps_runner/playbooks/onboard.yml`. |
| `docs/governance/testing-governance.md` | M | §3 decision table row updated to vps_runner (with mention of `test-vps-runner-integration` as W-R18 bottom line); §4.1 `make verify` description updated to `vps-runner-syntax` + `test-vps-runner`; §4.1 added `test-vps-runner` + `test-vps-runner-integration` + `vps-runner-syntax` rows (replacing vps-manager rows); §4.2 CI dependency graph updated. |
| `docs/reference/test-specs/INDEX.md` | M | TSVS-VPS-MANAGER-UNIT-001 marked **Retired 2026-05-16** with strikethrough; new `TSVS-VPS-RUNNER-UNIT-001` row registered (L1+L2 / `make test-vps-runner` + `make test-vps-runner-integration` / Active / points at feature-map §Tests anchor). §3 surface map updated. §5 history row added. |
| `TODO.md` | M | TASK-009 moved to Completed table with R4-b changelog link; TASK-010 flipped from PAUSED to **READY (▶️)** per its resume condition (vps-manager-v2 cutover done) — priority promoted to P0; recommended vps_runner as reference impl. |
| `docs/reference/feature-map/vps-runner.md` | M | Status updated to "Cutover complete"; dropped legacy plugin cross-reference and "Round 4-b pending" item; §Cross-references — round4-b changelog link added; §Open / Pending — replaced cutover-row with `runtime/state/vps_inventory.yml` operator note. |
| `docs/operations/vps-runner.md` | M | Removed "并存" lead paragraph; §1 dropped "取代了 vps_manager..." trailer; §7 retitled "设计沿革" (历史对照 only for understanding leftover artifacts); `runtime/state/vps_inventory.yml` paragraph rewritten as operator cleanup option. |
| `plugins/vps_runner/__init__.py` | M | Module docstring no longer references "legacy vps_manager plugin"; points at feature-map spec instead. |
| `plugins/vps_runner/vps_runner.py` | M | Module docstring no longer points at plan-2026-05-16; points at feature-map spec. |
| `plugins/vps_runner/playbooks/{onboard,modify,remove}.yml` | M | Top-of-file comment no longer says "rewritten from plugins/vps_manager/playbooks/..." (deleted path reference). |

**Local-only, not in git**: `plugins/vps_manager/__pycache__/` + `plugins/vps_manager/tests/__pycache__/` were never tracked but persisted as on-disk leftovers after the `git rm`; cleared with `rm -rf plugins/vps_manager/` after the staged delete. `runtime/state/vps_inventory.yml` + `runtime/state/tasks/` are NOT tracked and **NOT removed** — operator-owned dev artifact; user to clean locally at will.

## 2. Intent per change

### 2.1 Plugin deletion
A direct `git rm -r plugins/vps_manager/` after Round 3 reached feature-complete and Round 4-a wired the new plugin into every cross-cutting doc. The git history retains every file at commit `83f3107 ← 90a861c` boundary; no information is lost — only removed from the current tree where it actively misleads future readers/operators.

### 2.2 Makefile pruning
Both `.PHONY` and `verify` chain are now coherent — no dangling targets. The 5-line `VPS_EXTRA_GOAL` helper block (allowed `make vps-recover hk-d13` shorthand) was the only place in the Makefile that knew the user's alias namespace; gone with the targets it served.

### 2.3 Doc-historical references kept (deliberate)
- `CHANGELOG.md [Unreleased]` keeps the **VPS Manager plugin MVP (2026-05-14)** entry with a "REMOVED in Round 4-b above" tag — it's a historical record of what was in the project, exactly what CHANGELOG.md is for.
- `docs/reference/test-specs/INDEX.md` keeps the Retired TSVS row with strikethrough — the project's existing state-machine pattern requires retired TSVS IDs to remain registered for traceability.
- `docs/reference/investigations/IVG-DEEP-AUDIT-2026-05-15.md` — historical IVG, references vps_manager as point-in-time context. IVGs are immutable per workspace W-R10 doc-naming convention.
- `docs/reviews/feat-vps-manager-v2/` round changelogs — historical artifacts of the work itself.
- The branch name `feat/vps-manager-v2` (note: "v2" — the rewrite that DELETES "v1" in this round) appears in 4 governance docs as the source-of-truth identifier for this work; deliberately preserved.

### 2.4 vps_runner code/doc comment cleanup
The migration-history one-liners ("rewritten from plugins/vps_manager/...") in 3 playbook headers + 2 Python docstrings + 2 doc files were pointing at deleted paths — actively misleading and exactly the kind of "comment that ages worse than no comment" the project CLAUDE.md warns against. Replaced with pointers to the feature-map spec, which is the durable artifact.

### 2.5 TASK-010 unblock
TASK-010 (reality-migration) had its resume condition as "vps-manager-v2 round 4 cutover 完成". Condition met → flipped to READY with vps_runner suggested as the same-paradigm reference implementation.

## 3. Explicit "NOT done" list

- **3-host concurrency real test** — still blocked on `deploy@82.152.164.147 Permission denied (publickey)`. The cutover happened *before* this verification per user's explicit accept-trade-off statement; it is now a TASK-009 follow-up rather than a cutover gate.
- **`docker_host` / `deploy_compose` parity in vps_runner** — legacy actions not yet ported. Scope as separate task if/when needed (vs. delegating to a stand-alone docker-compose role).
- **runtime/state/ local cleanup** — operator's call, intentionally left.
- **CI workflow check for vps_manager references** — grep confirmed 0 hits in `.github/workflows/` even before cutover (legacy plugin never made it into CI). No further action needed.
- **TSVS-VPS-RUNNER-UNIT-001 dedicated spec file** — INDEX entry points at `feature-map/vps-runner.md#tests` rather than its own spec file. Lighter; if future work wants a standalone spec it can be added under `docs/reference/test-specs/vps-runner-unit.md`.

## 4. Self-check results (W-R15 three gates)

### Iteration 1 (clean — no fixes needed)

- **Gate 1 (Direction)**:
  - ✅ `plugins/vps_manager/` directory absent from working tree (verified via `ls plugins/` → only `vps_runner`)
  - ✅ `make help` lists 0 legacy vps-manager targets; 3 vps-runner targets present
  - ✅ All substantive references to vps_manager removed; remaining mentions are historical (CHANGELOG entries, retired TSVS row, IVG records, branch name, round-changelog cross-references) — verified per-file count and reviewed each remaining hit
  - ✅ vps_runner code no longer references deleted paths in comments/docstrings
- **Gate 2 (Syntax)**:
  - ✅ `python -m py_compile plugins/vps_runner/*.py plugins/vps_runner/tests/*.py` → exit 0
  - ✅ `ansible-playbook --syntax-check` on all 4 vps_runner playbooks → exit 0
  - ✅ `ansible-lint --profile production` on playbook directory → **0 failures, 0 warnings** (10/10 files passed)
- **Gate 3 (Functional)**:
  - ✅ `pytest plugins/vps_runner/tests/` → **22 passed in 11.03s** (incl. integration test against TEST-NET-1)
  - ✅ `make help` parses Makefile + lists exactly the 3 expected vps-runner targets (no legacy / no dangling)

**Iteration count: 1**. W-R17 cap respected.

## 5. CLAUDE.md / governance updates (this round)

None — the cutover is a one-time destructive operation, not a methodology change. The W-R7 deviation-tracking already documented why the cutover happens without SSH-verified parity (user trade-off accept).

## 6. Follow-Up / Next Steps

**Immediately doable (claude)**
- **TASK-010 reality-migration** — now READY (per TODO.md). User decides whether to resume immediately or do something else first. Plan in `docs/reviews/feat-reality-migration/plan-2026-05-15.md`; vps_runner is the reference implementation for the ansible-runner paradigm.
- **Optional polish (not blocking)**:
  - merge `feat/vps-manager-v2` → `dev` (or hold for further verification on user request — currently local-only)
  - clean `plugins/vps_runner/playbooks/*.yml` line-length warnings (33 non-blocking)
  - clean `INDEX.md` long-table line warnings (semantic only)

**Immediately doable (user)**
- Investigate `deploy@82.152.164.147 Permission denied (publickey)` — unblocks live `audit --limit <≥2 hosts>` 3-host concurrency verification (TASK-009 follow-up gate, no longer cutover-gating)
- Decide whether to delete `runtime/state/vps_inventory.yml` + `runtime/state/tasks/` locally (legacy artifact, no longer read)
- Branch hygiene: merge `feat/vps-manager-v2` → `dev` when ready (currently 4 commits ahead: round 2 / round 3 / round 4-a / round 4-b)

**Blocked**
- Live 3-host concurrency verification (waiting on SSH)

**Deferrable**
- `docker_host` / `deploy_compose` parity in vps_runner (scope when need surfaces)
- `inventory/vps_runner/{stag,prod}/host_vars/` real-node migration (no stag/prod VPS yet)

---
*Round 4-b closes feat/vps-manager-v2. The branch has 4 commits ahead of dev: round 2 (ansible-runner scaffold + list/audit), round 3 (lifecycle CLI + tests), round 4-a (docs + Make UX + shim flatten), round 4-b (destructive cutover). TASK-009 lifecycle: registered → planned → built → documented → cutover. TASK-010 unblocked.*
