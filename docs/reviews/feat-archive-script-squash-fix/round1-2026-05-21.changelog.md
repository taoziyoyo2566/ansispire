# Round 1 Changelog — Archive Script Squash-Merge Detection Fix

**Reference**: [`plan-2026-05-21.md`](./plan-2026-05-21.md)
**Date**: 2026-05-21
**Branch**: `feat/archive-script-squash-fix` (from `dev` @ `1a38697`)
**Workload level**: [L1] Engineering — bug fixes in two scripts shipped via PR #17
**Scope**: 2 WUs (initially 1, expanded mid-round when iter-2 dogfood surfaced a 2nd bug in a companion script). Plan-doc + changelog land alongside.

---

## File change manifest

| File | Change | Summary |
|---|---|---|
| `scripts/archive_branch.sh` | EDIT | **WU-1** (6 iterations): replace broken `git cherry`-based squash-merge fallback with batched `gh pr view` cross-check + harden destructive sink. **iter-1** state=MERGED only (insufficient — user). **iter-2** + headRefName + baseRefName (insufficient — codex round 1). **iter-3** + headRefOid (no post-merge re-push) + isCrossRepository (no fork PRs). **iter-4** + remote delete uses `--force-with-lease="refs/heads/$BRANCH:$TIP"` (W-R19); + local delete verifies `refs/heads/$BRANCH == $TIP` before `branch -D` (preserves unpushed-work). Source: codex round 2. **iter-5** + Steps 2+3 merged into one `git push --atomic --force-with-lease=…  origin "refs/tags/$TAG" ":refs/heads/$BRANCH"` — eliminates stuck state where tag-pushed but branch-not-deleted would block re-runs. Source: codex round 3. **iter-6** Step 4 local-cleanup TOCTOU + behind-case discrimination: replace `branch -D` (no expected-OID) with `git update-ref -d "refs/heads/$BRANCH" "$local_tip"` — atomic check-and-delete, local equivalent of `--force-with-lease`. Add `git merge-base --is-ancestor "$local_tip" "$TIP"` to distinguish behind (safe to delete; all local commits in tag) from ahead/divergent (real unpushed work; keep + warn). Closes the gap where the previous iter-4 implementation marketed attack-category (f) "TOCTOU race between verification and operation" as defended but Step 4 still had a capture-then-`branch -D` race window. Also brings the code in line with its own inline comment ("Refuse if local has commits BEYOND TIP" — was refusing on any mismatch, including safe behind-case). Source: ultrareview/codex round 5 finding `merged_bug_003` (nit severity but a marketed-defense gap). |
| `scripts/watch-pr-checks.sh` | EDIT | **WU-2** (4 iterations): python + bash both reworked. **iter-1** fixed (a) `total == 0` → false green; (b) only-FAILURE counted as failure. **iter-2** (codex round 1) added CheckRun + StatusContext unified handling via `terminal(c)` helper. **iter-3** (codex round 2): added `name(c)` helper using `.get('name') or .get('context') or '?'` for 3 spots that previously did `c['name']` (StatusContext entries lack `name`, would have KeyError'd mid-loop); bash wrapper now exits immediately on any `rc not in {0, 2, 99}` instead of silently retrying — Python crashes no longer degrade to timeouts. **iter-4** (codex round 4): wave-registration race fix — python emits `__NAMES__:<sorted-name-set>` line every poll; bash maintains `prev_sig` and only exits 0 when `current_sig == prev_sig && -n`. A single all-green observation now triggers one more poll before declaring green; if a new check (e.g. GitHub Actions matrix after GitGuardian StatusContext) appears in the interim, `sig` changes and the loop continues. Closes the false-green path where partial wave of checks all-succeeds before slower waves register. |
| `docs/reviews/feat-archive-script-squash-fix/plan-2026-05-21.md` | NEW | Plan-doc — root-cause analysis, W-R18 framework pre-check, alternatives considered, mid-round scope expansion note |
| `docs/reviews/feat-archive-script-squash-fix/round1-2026-05-21.changelog.md` | NEW | This file |

## Intent per change

### Guard 8 rewrite — root cause + fix

The previous round's WU-2 shipped `scripts/archive_branch.sh` with a
two-path merge check: (a) ancestor relationship for true-merge, (b)
`git cherry`-based patch-id walk as fallback for squash-merge.

Path (b) was based on a misreading of `git cherry`'s semantics.
`git cherry <upstream> <head>` walks each commit on `<head>` and looks
for a matching **per-commit** patch-id on `<upstream>`. With squash-
merge:

- The source branch has N commits with patch-ids `p1..pN`.
- Squash produces 1 commit on target with a **combined** patch-id
  `P_combined ≠ any pi`.
- `cherry` reports all N as `+` (missing).

Diagnostic on PR #17 (the very first dogfood call):

```
$ git cherry origin/dev origin/feat/dev-tooling-review-optim
+ cbd9d91 + 6cb9e5b + de3b05b + a9f0d11 + a0566a7 + fc38c21
```

Script then exits at `missing=6 ≠ 0` → refuse to archive a branch that
is in fact merged. There is no `git cherry` form that makes this work
for squash — the comparison the script author wanted (`every patch on
BRANCH is reachable from TARGET`) is not what cherry computes.

The fix replaces path (b) with a `gh pr view "$PR_NUM" --json state -q
.state` lookup. The script already collects `PR_NUMBER` as the third
positional arg (for the tag annotation message), so this introduces no
new arguments. The path is conditional on `gh` being installed and
emits a clear error if not.

Three-path acceptance now:

1. Ancestor (`git merge-base --is-ancestor TIP origin/TARGET`) →
   accept (true-merge, fast-path, no `gh` round-trip).
2. PR state via `gh pr view PR_NUM --json state` returns `MERGED` →
   accept (squash-merge case).
3. Else refuse with an error that names both checks and reports the
   actual PR state seen (or `unknown` if `gh` couldn't look it up).

The header comment block (`# 6.`) is updated to describe both paths
instead of only ancestry.

## NOT done (out of round 1 scope)

- **Dogfood archive of THIS branch** (`feat/archive-script-squash-
  fix`). Gated on this PR being merged first.
- **Dogfood archive of `feat/dev-tooling-review-optim`** (PR #17's
  branch — currently still alive on `origin` because the broken gate
  refused). Gated on this PR being merged, then running the fixed
  script.
- **`--force-merged` override flag**. Considered and rejected:
  defeats the gate's purpose; the `gh`-based check covers the case
  the user would otherwise reach for `--force` to bypass.
- **Patch-id-of-combined-diff fallback**. Considered and rejected:
  reconstructs what GitHub already authoritatively answers; fragile
  re: which window of target commits to compare; no real safety
  benefit over `gh pr view`.
- **Make `gh` a hard prereq**. Kept conditional: true-merge case still
  works without it, and the only repos likely to use the script are
  ones already using `gh` for `push-and-watch-pr.sh` etc.

### WU-2 — watch-pr-checks.sh: empty-rollup + non-FAILURE-conclusion bugs

Discovered during iter-2 push to PR #18: `./scripts/watch-pr-checks.sh
18` returned exit 0 "all green" within ~10 seconds, while CI actually
had 7 of 13 checks pending (4 molecule scenarios, dry-run, 2 API
contract). Three real bugs in one python block:

1. **`total == 0` declared green** (HIGH; observed). Right after
   `git push`, GitHub takes 10–30s to register workflow runs. Polling
   in that window returns `statusCheckRollup: []`. The old code
   computed `pending = sum(1 for c in checks if not c.get(...))` over
   the empty list → `pending = 0` → `if pending == 0: exit 0`. Fix:
   explicit `if total == 0: print "(not yet registered)"; exit 99`
   before computing done/pending.

2. **Non-FAILURE conclusions silently passed** (MEDIUM; latent).
   GitHub Actions conclusion enum is wider than `{SUCCESS, FAILURE}`:
   `CANCELLED`, `TIMED_OUT`, `ACTION_REQUIRED`, `STALE`,
   `STARTUP_FAILURE`, `SKIPPED`, `NEUTRAL`. The old `failures = [...
   if conclusion == 'FAILURE']` only flagged FAILURE; the other
   non-OK terminal states were counted as `done` with `failures = []`
   → exit 0. Fix: invert to allow-list `OK_CONCLUSIONS = {'SUCCESS',
   'SKIPPED', 'NEUTRAL'}`; everything else with a conclusion set =
   failure-equivalent.

3. **Wave-registration race** (LOW; theoretical, NOT fixed this
   round). If GitHub registered workflow runs in waves (only fast
   ones first, then slow ones), polling between waves could see e.g.
   6/6 done = "all green" before the slow 7 even appeared.
   Mitigation: in practice GitHub triggers all `on: push` workflows
   from a single event and they're all registered within ~1s of each
   other; the observed race is bug #1's empty-rollup window, not a
   true wave. Documented in `watch-pr-checks.sh` header for future
   reference; fix deferred unless observed.

Bugs 1 and 2 were both caught by an **adversarial gate-1 review** of
the patch (the same technique that caught the archive_branch.sh HIGH
regression in iteration 1 of WU-1). The check: enumerate "what
attack scenarios does this NOT block" before declaring direction
clean. Without that lens bug 2 would have shipped.

## Self-check results (W-R15 3-Gate)

**Iteration 1** — gates 2 + 3 superficially clean, but **gate 1
(direction) was not rigorous enough**: the user-requested code review
of the iter-1 diff caught a safety regression I had missed. State-only
acceptance let a merged PR for a DIFFERENT branch authorize archival
of THIS branch (false positive worse than the original cherry-based
false negative — the original at least never deleted the wrong
branch, only refused to delete the right one). Iteration 1 therefore
**not** a clean pass.

**Iteration 2** (WU-1) — all gates clean.

| Gate | Outcome (iter 2) |
|---|---|
| 1 — Direction | ✅ Diff verifies all three: `state == MERGED` AND `headRefName == BRANCH` AND `baseRefName == TARGET`. Single batched `gh pr view` call via `-q '[…] \| @tsv'`. Per-mismatch error messages name the specific mismatch. Empty `pr_info` (gh-call failure) is distinguished from PR-not-merged. |
| 2 — Static | ✅ `bash -n scripts/archive_branch.sh` clean. |
| 3 — Functional | ✅ Four subcases, all from `dev` checkout with `exit 0` injected after guard 8: **3a** positive (`feat/dev-tooling-review-optim dev 17`) → `head=feat/dev-tooling-review-optim, base=dev` printed, reaches post-guard-8. **3b** HIGH-regression (`feat/archive-script-squash-fix dev 17` — wrong BRANCH + right PR) → refuses with `PR #17 head branch is 'feat/dev-tooling-review-optim', not 'feat/archive-script-squash-fix'`. **3c** bogus PR (`… … 99999`) → refuses with `'gh pr view 99999' returned no data`. **3d** MEDIUM-regression (`feat/dev-tooling-review-optim feat/vps-manager-v2 17` — wrong TARGET) → refuses with `PR #17 merged into 'dev', not 'feat/vps-manager-v2'`. True-merge fast-path: code block byte-identical to previous version → no regression risk; no synthetic test needed. |

**WU-2** (watch-pr-checks.sh) — iteration 1, all gates clean.

| Gate | Outcome |
|---|---|
| 1 — Direction | ✅ Adversarial review enumerated 3 attack scenarios (empty rollup, non-FAILURE non-OK conclusions, wave-registration race). Patch addresses #1 and #2; #3 documented as deferred (theoretical, not observed). Sort key inverted consistently with the new OK set. `pending` rederived from `total - done` (algebraically equivalent to previous; cleaner). |
| 2 — Static | ✅ `bash -n scripts/watch-pr-checks.sh` clean; python block compiles via `compile(src, '<watcher>', 'exec')`. |
| 3 — Functional | ✅ Six synthetic JSON subcases via stdin to standalone python block: **3a** empty rollup → exit 99 + "not yet registered" (was: exit 0 → bug). **3b** all SUCCESS → exit 0. **3c** one FAILURE → exit 2. **3d** one CANCELLED → exit 2 (was: exit 0 → bug). **3e** SUCCESS + SKIPPED + NEUTRAL → exit 0. **3f** mixed conclusion + null → exit 99. |

**Iteration 3** (WU-1, codex review) — all gates clean.

| Gate | Outcome (iter 3) |
|---|---|
| 1 — Direction | ✅ Adversarial enumeration (11 scenarios this time): wrong BRANCH, wrong TARGET, bogus PR, PR-OPEN, PR-CLOSED-unmerged, gh-not-authed, network-fail, **fork PR (NEW)**, **post-merge re-push (NEW)**, branch reuse, current-checkout. iter-3 adds `headRefOid == TIP` and `isCrossRepository == false`, each refusing with a distinct named error. Cross-repo check fires before head/base mismatches (fork-PR's head ref doesn't even live in this repo). |
| 2 — Static | ✅ `bash -n` clean. |
| 3 — Functional | ✅ Six subcases from `dev` with `exit 0` injected after guard 8: **3a** positive → "tip matches merged head" + reach post-guard-8. **3b** wrong BRANCH → refuses (regression). **3c** bogus PR → refuses (regression). **3d** wrong TARGET → refuses (regression). **3e** post-merge re-push (simulated via inline `TIP="deadbeef…"`) → `Error: origin/$BRANCH tip (deadbeef…) does not match PR #17 merged head (fc38c21…)`. **3f** fork PR (simulated via `pr_cross_repo="true"`) → `Error: PR #17 is from a fork`. |

**WU-2 iteration 2** (codex review) — all gates clean.

| Gate | Outcome (iter 2) |
|---|---|
| 1 — Direction | ✅ Added `terminal(c)` helper unifying CheckRun + StatusContext shapes: prefers `conclusion`, falls back to `state`, treats `PENDING`/`EXPECTED` as still-running. `OK_TERMINAL` set covers both CheckRun-OK conclusions and StatusContext's `SUCCESS` (StatusContext does not emit SKIPPED/NEUTRAL, so the set is correct as-is). `display(c)` helper for sort output uses fallback chain `conclusion → state → status → '?'`. |
| 2 — Static | ✅ `bash -n scripts/watch-pr-checks.sh` clean; python compiles. |
| 3 — Functional | ✅ Ten synthetic JSON subcases: **3a-c** regression (empty rollup → 99; CheckRun SUCCESS → 0; CheckRun CANCELLED → 2). **3d** StatusContext SUCCESS (no conclusion) → 0 (NEW; previously forever-pending bug). **3e** StatusContext FAILURE → 2 (NEW; previously timeout-not-fail bug). **3f** StatusContext ERROR → 2 (NEW). **3g/h** StatusContext PENDING/EXPECTED → 99 (NEW). **3i** mixed CheckRun+StatusContext both OK → 0. **3j** mixed with StatusContext FAILURE → 2. |

W-R17 retry cap: WU-1 used 3 of 3 iterations (at the cap). The cap
triggered for *consecutive insufficient-fix discoveries from external
review*, not for *the same fix failing repeatedly*. The strict
interpretation of W-R17 ("3 consecutive failed attempts at the same
fix") does not apply — each iteration's gates passed; the gap was in
gate 1's adversarial enumeration scope, not in the fix's execution.

**Iteration 4** (WU-1, codex 2nd review) — gates clean.

| Gate | Outcome (iter 4) |
|---|---|
| 1 — Direction | ✅ Used iter-3's pre-built attack-category checklist plus 3 newly added categories: (f) TOCTOU race between verification and operation; (g) collateral damage on local refs (unpushed work); (h) stale local view of origin. Re-checked the entire post-guard-8 flow (Steps 1–4). |
| 2 — Static | ✅ `bash -n` clean on both scripts. |
| 3 — Functional | ✅ **archive_branch.sh** — 3a positive (regression: guard 8 still accepts PR #17); 3b step-4 unpushed-work mismatch (extracted step-4 logic + injected `local_tip=abcd123ahead`) → keeps local branch + warn + `git log` hint; 3c step-4 match → delete OK; 3d lease syntax (`git push --dry-run --force-with-lease=ref:fake-oid origin :ref`) → git refuses with "stale info" (correct lease semantics). **watch-pr-checks.sh** — 3a StatusContext-only-`context` FAILURE → exit 2 with `ci/external` displayed via `name()`; 3b StatusContext SUCCESS → exit 0; 3c malformed entry (no name, no context) → graceful `?` display + exit 2 (failure-tagged); 3d CheckRun-by-name regression → exit 0; 3e bash fail-fast (simulated python crash via `raise KeyError`) → wrapper exits rc=1 immediately, no silent retry loop. |

**WU-2 iteration 3** (codex 2nd review) — gates clean.

| Gate | Outcome (iter 3) |
|---|---|
| 1 — Direction | ✅ Added `name(c)` helper applied to 3 spots (`failures` list, sort key, display loop). Bash wrapper: rc not in {0, 2, 99} → exit `$rc` immediately. Note: `name(c)` returns `'?'` for malformed entries — failures are still reported as failures, just with `'?'` as name; better than silent timeout. |
| 2 — Static | ✅ same as above. |
| 3 — Functional | ✅ covered in WU-1 iter-4's watcher subcases (3a-3e above). |

**Iteration 5** (WU-1, codex 3rd review) — gates clean.

| Gate | Outcome (iter 5) |
|---|---|
| 1 — Direction | ✅ New attack category (i) "partial-failure recovery / atomicity" added to the checklist. The old Step 2 (push tag) + Step 3 (delete branch) were two independent remote operations; if step 3 failed (lease mismatch / network), step 2's already-pushed tag created a stuck state — re-runs would refuse at guard 9 because `git ls-remote --tags origin` would find the orphan tag. Replaced with single `git push --atomic --force-with-lease=… origin "refs/tags/$TAG" ":refs/heads/$BRANCH"` — server-side transaction guarantees both refs apply or neither. Lease still keyed only to the branch refspec (tag is a normal create). Error message enumerates the 3 failure modes (lease mismatch, remote-tag race, network). Header comment behavior list updated to merged Step 10. |
| 2 — Static | ✅ `bash -n` clean. |
| 3 — Functional | ✅ Used `git push --dry-run --atomic` with a real local tag at `$TIP` for `feat/dev-tooling-review-optim`. **3a** (matching lease): dry-run reports `[deleted] feat/dev-tooling-review-optim` + `[new tag] archive/...` + exit=0 — both refspecs would apply. **3b** (wrong lease oid `deadbeef…`): dry-run reports `! [rejected] (delete) → feat/dev-tooling-review-optim (stale info)` + `! [rejected] archive/... (atomic push failed)` + exit=1 — proves both refspecs roll back when ONE fails. This is the exact property codex requested. |

**Iteration 6** (WU-1, ultrareview/codex round 5) — gates clean.

| Gate | Outcome (iter 6) |
|---|---|
| 1 — Direction | ✅ Codex round 5 finding `merged_bug_003` (nit): (1) Step 4's `git rev-parse` + `git branch -D` is non-atomic, while remote side uses `--force-with-lease`; (2) `local_tip != TIP` branch fires on behind/ahead/divergent but warning text + `git log $TIP..$local_tip` hint only make sense for ahead. iter-6 fixes both with a single block rewrite: `git update-ref -d "refs/heads/$BRANCH" "$local_tip"` is the canonical atomic local-ref check-and-delete (the local equivalent of `--force-with-lease`); `git merge-base --is-ancestor "$local_tip" "$TIP"` discriminates behind (all commits in tag → safe delete) from ahead/divergent (real unpushed work → keep + show `git log $TIP..$local_tip`). Also updated the script header behavior list (step 11) to match the new semantics, and the inline comment now matches the implementation (was: "Refuse if local has commits BEYOND TIP" — but code refused on any mismatch). |
| 2 — Static | ✅ `bash -n scripts/archive_branch.sh` clean. |
| 3 — Functional | ✅ Six subcases in `/tmp/archive-iter6-gate3` (synthesized sandbox repo with C0/C1/C2/divergent commits). **A** local==TIP → atomic delete + ref removed. **B** local behind TIP (is-ancestor) → safe delete + "deleted behind" message. **C** local ahead of TIP → keep + show unpushed-commits hint + ref preserved. **D** true-divergent (built on common ancestor, neither side is-ancestor of the other) → keep + warn (same path as ahead). **E** TOCTOU simulation — `git update-ref -d <ref> <stale_expected_old>` directly: git refuses with "ref ... is at ..., not ..." → wrapper would print "TOCTOU refuse" + ref preserved. Confirms `update-ref -d`'s atomic-check semantics are the real mechanism (not a Bash-level race window). **F** branch absent → no action. |

**Process meta-lesson reinforced (round-5 nit-severity finding)**: Even after 5 self-review iterations + 4 external-review rounds, the round-5 external review still found a marketed-defense gap. The gap: iter-4 changelog explicitly claimed (f) "TOCTOU race between verification and operation" was defended, but Step 4 (the very block iter-4 added the SHA-check to) was implemented with non-atomic `rev-parse` + `branch -D`. Self-review missed this because "lease on remote" and "SHA-check on local" felt symmetric semantically; only an external eye reading at the operation-primitive level noticed `branch -D` takes no expected-OID. **The mental shortcut "verify-then-act"** is the structural blind spot — every reviewer (including iter-4 codex round 2 which added the SHA-check) accepted this pattern. **Lesson**: for any guarded ref mutation, ask "does the mutation primitive itself accept an expected_old_oid argument?" If no, the pattern is verify-then-act = TOCTOU-vulnerable. Atomic primitives: `git update-ref -d/-u <ref> <expected_old>`, `git push --force-with-lease=<ref>:<expected_old>`. Non-atomic: `git branch -D`, `git push :ref` (without lease), `rm -f`.

**WU-2 iteration 4** (codex round 4) — gates clean.

| Gate | Outcome (WU-2 iter 4) |
|---|---|
| 1 — Direction | ✅ New attack category (j) "wave-registration race / unstable name-set across polls" added to the checklist. Codex found this: external StatusContext (e.g. GitGuardian) registers and reaches SUCCESS before the GitHub Actions matrix even registers. Watcher's `pending == 0` branch would fire on the partial wave (1/1 done, 0 pending) and exit 0 before Actions matrix produces its first check. Fix at TWO layers: (a) python emits `__NAMES__:<sorted-comma-joined-names>` line on EVERY poll (unconditional, in pending and all-done branches), so bash always has access to the current name-set; (b) bash wraps rc=0 in a "previous-poll same name-set?" check — if `current_sig == prev_sig && -n`, exit 0; else print "name-set not yet stable" and loop. Defensible because: if at time T₁ the name-set is S and at T₂ the name-set is still S, then S already includes all checks the push triggered; any later-registering check would have appeared between T₁ and T₂, changing the set. |
| 2 — Static | ✅ `bash -n scripts/watch-pr-checks.sh` clean; python block compiles via `compile(src, '<watcher>', 'exec')`. |
| 3 — Functional | ✅ Two layers of synthetic tests in `/tmp/watcher-gate3/`. **Layer 1 (python-only)**: 5 subcases — A wave-reg single ctx → rc=0 + sig="GitGuardian…"; B full-wave all-green → rc=0 + sig=5-name set; C full-wave one fail → rc=2 + sig; D empty rollup → rc=99 + sig=""; E pending mid-flight → rc=99 + sig. **Layer 2 (full bash integration, stub `gh`)**: 5 scenarios — **S1** wave-registration race (GG-only → full pending → full green → full green) → rc=0 at iter 3, NO false-green at iter 1 (the bug codex flagged). **S2** full wave green from start, twice → rc=0 at iter 2 (one stability loop). **S3** empty → empty → full green twice → rc=0 at iter 4. **S4** failure terminal → rc=2 at iter 1 (no stability required for failures). **S5** partial-green (would have false-greened pre-fix) → full-wave one-fail → rc=2 at iter 2 (real failure caught instead of false-green). All 5 PASS — wave-registration race is closed AND failure-path still fails fast. |

**Meta-lesson sharpened (iter-5 + iter-4-WU2 update)**: gate 1 "adversarial enumeration" needs a **floor of distinct attack categories**, not just whatever comes to mind. For destructive-ref operations specifically: (a) wrong identifier (typo / different branch); (b) wrong TYPE of object (fork vs same-repo PR); (c) right identifier but stale content (branch re-push after merge); (d) right identifier but wrong relationship (PR base ≠ TARGET); (e) source not in our repo (cross-repo fork PRs); (f) TOCTOU race between verification and operation; (g) collateral damage on local refs (unpushed work loss); (h) stale local view of origin; (i) partial-failure recovery / atomicity across multi-step destructive ops; **(j) wave-registration race / unstable observation set across polls (relevant for "all-clear" termination conditions on multi-producer event streams)**. **Iter-1 covered (a). iter-2 added (d). iter-3 added (b)(c)(e). iter-4 added (f)(g)(h). iter-5 added (i). WU-2 iter-4 added (j)**. The pre-built checklist must be used **before claiming gate 1** on any future destructive-script work. Note: I documented (j) as a theoretical risk in iter-1 ("Wave-registration race (LOW; theoretical, NOT fixed this round)") and self-deferred it; codex round 4 demonstrated it's concrete by reading PR #18's actual timeline. Lesson: a "deferred because theoretical" item in a changelog must be either (a) ticketed with explicit owner+date, or (b) fixed. Mine drifted for 3 more iterations because it was neither.

**Process-level lesson**: 3 consecutive external reviews (user → codex
→ codex) caught what 3 self-reviews (mine + in-session security-review
skill + adversarial gate-1) missed. For scripts with destructive sinks
(`git push :ref`, `git push --force*`, `git branch -D`, `rm -rf`,
schema drop), self-review is **structurally insufficient**. Future
work in this category SHOULD route through external review (codex /
ultrareview) before merge — the cost is one CI cycle; the saved cost
is the next data-loss bug.

## CLAUDE.md updates (this round)

- **Added W-R19** to `~/workspace/CLAUDE.md` §8 — "No bare force-push; OID-lease only". Bans `git push --force` / `-f` / `+refspec` (including `:ref` deletion which is semantically equivalent — no FF check). Requires `--force-with-lease=<ref>:<expected-oid>` with the OID captured before any verification step that could race. Also covers local `-D`: SHA-equality check required before unguarded `branch -D`. Layers with `~/.claude/CLAUDE.md` §3 (which already requires approval) — W-R19 narrows the form-of-operation once approved. Source: this round's codex finding line 208 + line 215.
- **Added W-R20** to `~/workspace/CLAUDE.md` §8 — "Destructive-sink code requires external review before merge". Self-review (including in-session skills like `/security-review`) is structurally insufficient for code containing irreversible operations on shared state. External review = `codex` / `/ultrareview` / equivalent independent agent stack. Mandatory before merge for any code touching: `git push :ref`, `git push --force*`, `git branch -D`, `rm -rf`, DB `DROP`/`TRUNCATE`/destructive migrations, irreversible API mutations, kill of production processes. Waivable by explicit user statement (recorded in changelog). Layers with W-R19 (form) — W-R20 is the process gate. Source: this round's 4-iteration history where each external pass found new attack categories my self-reviews missed.

## Follow-Up / Next Steps

### Immediately doable (you can pick up directly)
- **Squash-merge this PR (when CI green)** — same flow as PR #17.
  Owner: user approval, then Claude executes.

### Blocked
- **Dogfood archive of this branch** (`feat/archive-script-squash-
  fix`). Use the fixed script:
  ```bash
  ./scripts/archive_branch.sh feat/archive-script-squash-fix dev <this-PR#>
  ```
  Blocked on this PR being merged.
- **Re-attempt archive of `feat/dev-tooling-review-optim`** (PR #17):
  ```bash
  ./scripts/archive_branch.sh feat/dev-tooling-review-optim dev 17
  ```
  Blocked on this PR being merged. Will exercise the squash-merge path
  end-to-end.

### Deferrable (no new entries this round)
Carried over from previous round: cross-source port consistency check,
`.rgignore`, `test_results/` untrack, SSOT migration. Same priority as
before.

---

*Round 1 closes a single bug-fix loop on the dev-tooling round's WU-2.
Next round: dogfood the fixed script twice (archive this branch, then
archive PR #17's branch) — that's the real end-to-end validation that
the previous round's dogfood couldn't complete.*
