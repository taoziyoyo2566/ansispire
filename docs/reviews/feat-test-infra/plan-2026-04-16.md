# feat-test-infra — Architecture Exploration: Modular Test Infrastructure & Feature Toggle System

**Date**: 2026-04-16  
**Level**: Architecture  
**Type**: Exploration (no implementation this session)  
**Status**: Awaiting user review  
**Document path**: `docs/reviews/feat-test-infra/plan-2026-04-16.md`

---

## Context

This document captures an exploration for improving test infrastructure and introducing a feature-toggle mechanism in a topic-first doc layout (`feat-test-infra/`) per R12.

> Note: This file is reconstructed from session logs after an interrupted local WIP; some earlier narrative sections may be missing. The “Next Session Pickup — TODO” block below is preserved as the authoritative execution checklist.

---

## 9. Next Session Pickup — TODO

> Pick up here in the next session. Complete “Open Questions” first; they gate Phase 1.

### Step 1 — Decisions needed from user (before any implementation)

Answer these four questions (from the original exploration’s Open Questions section):

| # | Question | Options | Impact |
|---|----------|---------|--------|
| Q1 | Container connection | `privileged` systemd (realistic, ~60s) vs Docker plugin (fast, less realistic) | Affects all scenario `molecule.yml` platform blocks |
| Q2 | `ha` flag granularity | Single `ha: false` vs split `ha_primary / ha_secondary` | Affects `features.yml` schema |
| Q3 | Shared base vars path | `molecule/shared/group_vars/all/base.yml` (Molecule convention) vs `tests/fixtures/base.yml` (explicit) | Affects directory layout for Phase 1 |
| Q4 | Control plane provisioning | Via `bootstrap.yml` (reuse existing) vs raw Docker Compose (no Ansible dependency) | Affects `integration-control-plane` converge design |

### Step 2 — Phase 1 implementation (Engineering, small)

Once Q1–Q4 answered, implement the foundation skeleton:

- [ ] Create `inventory/group_vars/all/features.yml` (all flags false)
- [ ] Create `molecule/shared/group_vars/all/base.yml` (extract shared vars from the existing Molecule scenarios)
- [ ] Rename scenarios:
  - `common` → `unit-common`
  - `webserver` → `unit-webserver`
  - `database` → `unit-database`
  - `full-stack` → `integration-full-stack`
- [ ] Update each `molecule.yml` to inherit from `molecule/shared/` instead of inline vars
- [ ] Add Makefile targets: `test-unit`, `test-integration`, `test-scenario`, `test-all`
- [ ] Verify: `molecule test --all` still passes (no behavior change)
- [ ] Write `docs/reviews/feat-test-infra/phase1-YYYY-MM-DD.changelog.md`

### Step 3 — Phase 2 (Engineering, medium) — after Phase 1 passes

- [ ] Add `when: ansispire_features.<name> | default(false)` guards to each role's entry task
- [ ] Update scenarios to activate only needed features
- [ ] Add verify assertion: if a feature is disabled, its service must NOT be running

### Step 4 — Phase 3 (Architecture, large) — separate session

- [ ] Design `integration-control-plane` scenario topology in detail
- [ ] Implement Docker topology: semaphore + audit-sink + audit-relay
- [ ] Wire `bootstrap.yml` as converge playbook
- [ ] Verify: RBAC smoke + audit event chain

