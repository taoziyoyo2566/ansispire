# Round 1 Changelog — Drop AlmaLinux; RHEL family = Rocky Linux only

**Reference:** [`plan-2026-05-25.md`](./plan-2026-05-25.md)
**Branch:** `feat/vps-manager-v2` (in sync with `origin/dev` — verified `git log feat/vps-manager-v2..origin/dev` empty)
**Level:** 🔴 [L2] — platform support-matrix narrowing + one functional fix in `onboard.yml`
**Date:** 2026-05-25

---

## Summary

AlmaLinux removed from the supported platform matrix; the RHEL family is now **Rocky Linux 9+ only** (passive removal — no runtime reject, the shared `os_family == RedHat` path is unchanged). The standalone `onboard.yml` admin-group default was corrected from hardcoded `['sudo']` to a family-selected value (`wheel` on RedHat, `sudo` on Debian), matching the roles-path pattern.

---

## File change manifest

| File | Change | Intent |
| :--- | :--- | :--- |
| `plugins/vps_runner/playbooks/onboard.yml` | Added `vps_admin_group` (wheel if RedHat else sudo); `vps_managed.groups` default `['sudo']` → `[vps_admin_group]`; L528 comment de-Alma | **Functional**: Rocky onboard now joins `wheel`, not a spurious `sudo` group |
| `inventory/hosts.ini` | Removed `alma9` from `[targets_rhel]` | Drop Alma host |
| `inventory/vps_runner/dev/hosts.yml` | Removed `hk-alm9: null` | Drop Alma host |
| `inventory/vps_runner/dev/host_vars/hk-alm9.yml` | **Deleted** (carried a `failed` last_run) | Drop Alma host record |
| `roles/common/tasks/preflight.yml` | Tier-1 distribution check `['Rocky','AlmaLinux']` → `== 'Rocky'`; tier + family comments de-Alma | Narrow Tier-1 to Rocky; keep family hard-gate |
| `roles/common/vars/os/RedHat.yml` | Tier comments → Rocky-only | Doc accuracy |
| `roles/infra_baseline/vars/RedHat.yml` | Header comment de-Alma (`wheel` value untouched) | Doc accuracy |
| `roles/infra_baseline/tasks/main.yml` | L4, L66 comments de-Alma | Doc accuracy |
| `roles/infra_baseline/tasks/redhat.yml` | L3 comment de-Alma | Doc accuracy |
| `roles/common/tasks/security.yml` | L113 firewalld comment de-Alma | Doc accuracy |
| `roles/common/meta/main.yml` | EL `"9"` platform comment → Rocky-only | Galaxy metadata accuracy |
| `roles/common/README.md` | Removed AlmaLinux Tier-2 support row | Support matrix accuracy |
| `playbooks/deploy_target.yml` | Header comment de-Alma | Doc accuracy |
| `ARCHITECTURE.md` | L24 inventory taxonomy → 3-host fleet, AlmaLinux dropped note | Design truth sync |
| `README.md` | L94 baseline-VPS row de-Alma | Entry-point sync |
| `docs/reference/feature-map/multi-os-fleet.md` | L16 RHEL row → Rocky only | Feature-map sync |
| `docs/reference/feature-map/INDEX.md` | L108 + L177 → Rocky-only + drop note | Aggregate inventory sync (CLAUDE.md §0 mandatory) |
| `docs/reference/feature-map/vps-runner.md` | L26 `Rocky/Alma 9` → `Rocky 9` | Feature-map sync |
| `docs/reference/feature-map/test-infra.md` | L76 `Rocky Linux 9 / Alma 9` → `Rocky Linux 9` | **Caught by grep gate** — not in original manifest |
| `docs/operations/vps-runner.md` | L390 EPEL-error row `RHEL/Alma/Rocky` → `RHEL/Rocky` | Ops-doc sync |
| `CHANGELOG.md` | New `[Unreleased]` entry on top | Release-note sync (CLAUDE.md §0 #4) |
| `.claude/settings.local.json` | Removed dead `--limit alma9` permission entry | **Caught by grep gate** — config hygiene (workspace §6) |

---

## NOT done (explicitly out of scope)

- **No runtime reject** of AlmaLinux added (user chose passive removal; binary-compatible RHEL path stays tolerant).
- **Physical VPS decommission** (82.152.164.144) — user's manual action.
- **Historical records untouched**: `docs/reviews/_archive/**`, `feat-multi-os-target-fleet/**`, `feat-vps-manager-v2/round13-14 + plan-2026-05-24`, `reference-cn/snapshot-*`, and the *prior* `[Unreleased]` CHANGELOG entries (L53/L77/L82) that narrate what TASK-007 / the 2026-05-24 round did — these are accurate records of past work.
- **CentOS Stream / RHEL mentions kept**: user's instruction was "drop AlmaLinux"; CentOS Stream 9 (Tier 2) and generic "RHEL" family descriptors were left. See Next Steps if Rocky-only-literally is desired.

---

## Self-check (W-R15 3-gate, final iteration — all clean together)

| Gate | Result |
| :--- | :--- |
| **1. Direction** | All edits match approved plan; 2 surfaces the manifest missed (`test-infra.md`, `settings.local.json`) were surfaced by the gate-3 grep and fixed, then the loop restarted from gate 1. |
| **2. Syntax / lint** | `yamllint` (project-standard `yamllint .`) clean; `ansible-lint --profile production` **Passed, 0 failures (20 files)**; `settings.local.json` valid JSON (`json.load`). hosts.ini yamllint "error" was a category error (INI ≠ YAML) — project lint correctly ignores it. |
| **3. Functional** | `ansible-playbook --syntax-check` OK for `onboard.yml` + `deploy_target.yml`; `ansible-inventory --graph` shows `[targets_rhel]` = `rocky9` only; vps_runner dev inventory parses with no `hk-alm9`; **fact-trace** proves `os_family==RedHat → wheel`, `Debian → sudo`; final `grep -rni alma` over active surfaces = only intentional drop-notices + historical narrative. |

---

## CLAUDE.md updates (this round)

None. No new recurring feedback pattern emerged. Note: the manifest-vs-grep gap (2 missed surfaces caught by the functional grep gate) reinforces the existing **W-R15 gate-3** discipline and `[[feedback-proactive-failure-audit]]` — the grep-as-functional-gate worked exactly as intended; no new rule warranted.

---

## Follow-Up / Next Steps

**Immediately doable**
- (User) Commit + open PR `feat/vps-manager-v2 → dev` when ready. `onboard.yml` is a destructive-adjacent automation path but this change adds no destructive sink, so external review is optional (not W-R20-mandated).
- (User) Physically decommission / repurpose the ex-AlmaLinux VPS at `82.152.164.144` if no longer needed.

**Blocked / pending decision**
- **Rocky-only-literally?** If the RHEL family should be narrowed to *only* Rocky (dropping the kept CentOS Stream 9 Tier-2 + generic "RHEL" descriptors), that's a small follow-up — flag it and I'll do a focused pass. Left out this round to stay within the approved "drop AlmaLinux" scope.

**Deferrable**
- The prior `[Unreleased]` CHANGELOG entries still mention AlmaLinux as historical narrative; they will naturally roll into a release section unchanged. No action needed unless a release-notes cleanup is wanted.
